{
 "id": "VERDICT-F-77",
 "title": "Every non-dash argument of a generic mutator is treated as a path, so a mode or owner operand is resolved against the cwd: `chmod +x <scratch>/x.sh` is denied because `+x` resolves to `<checkout>/+x` - and so is `chmod +x .qa/x.sh`, inside the QA root the agent may write",
 "severity": "Minor",
 "priority": "P3",
 "status": "resolved",
 "failure_classification": null,
 "confidence": "proven",
 "fix_verified": true,
 "verification_test": "tests/test_hooks.py::test_a_mode_operand_is_not_a_path",
 "evidence": [
  "FIXED in 0.80.0. _MODE_FIRST = {chmod,chown,chgrp,install} (hooks/enforce_bash_scope.py:68); the _MUTATORS branch (:817-823) drops the first operand for these before treating the rest as paths.",
  "COUNTERFACTUAL (scratchpad/probe_f77.py): OLD hook at 4dc006a DENIED all of `chmod +x <scratch>/x.sh`, `chmod +x <checkout>/.qa/x.sh`, `chmod 755 <scratch>/x.sh`, `chown nobody <scratch>/x.sh`; NEW hook ALLOWS all four. CONTROL: `chmod 777 <checkout>/src/verdict_mcp/state.py` still DENIES under both — the file behind the mode is still a target.",
  "GUARD: tests/test_hooks.py::test_a_mode_operand_is_not_a_path (:1101) asserts the four allows incl. `.qa/x.sh`, and that a chmod of src/ still denies; green in the 1172 suite."
 ],
 "root_cause": {
  "mechanism": "chmod/chown's first operand is a mode/owner, not a path; the generic mutator loop resolved it against the cwd. 0.80.0 skips the first operand for _MODE_FIRST commands.",
  "fix_location": "code",
  "confidence": "proven"
 },
 "narrative": "RESOLVED, fix-verified by counterfactual. A chmod inside the QA root the tester may write is now allowed, and a chmod of the code under test is still denied."
}