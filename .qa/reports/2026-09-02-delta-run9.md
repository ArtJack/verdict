# QA report — verdict · run 9 (delta)

**VERDICT: pass with risks**

## Scope

- Range: `5dc59fb3071a76068e3ec5d7237cfad46bf46637..a6011ec465aaf1fe793c2e7f4f002fcb999770e9` · 21 files changed, 1492 insertions(+), 447 deletions(-)
- Branch: `main` · measured 2026-09-02T21:58:16Z
- Isolation check: **pass**

Delta run 9 over `5dc59fb..a6011ec` (v0.66.0 → v0.70.0, five commits, 21 files, +1492/−447), team mode, `.qa/` inside the repository. Four releases and one QA record commit are in the range: 0.67.0 and 0.69.0 are the two halves of a mutation campaign over `validate.py`, 0.68.0 fixes the three findings run 8 filed, and 0.70.0 closes the renderer half of VERDICT-F-36 and all four blind spots of VERDICT-F-13.

Effort went where the previous run's `next_run_focus` pointed and where this project's own findings cluster: `src/verdict_mcp/harness.py` (21.0 severity-weighted, 9 findings, 7 still open) and `src/verdict_mcp/state.py` (17.0, 7 findings, 7 open) took the reading budget, with the bash guard next because it is risk area 1 and the range touched it. Every one of run 8's eight open findings was examined; five are resolved and fix-verified, three carry forward.

One thing to read before anything else in this report: the first four re-injections this run performed reported the fixes verified when they had measured nothing at all. See VERDICT-F-43.

## Gates

| Gate | Result | Exit | Duration | Summary |
|---|---|---|---|---|
| `suite` | pass | 0 | 72.17s | 674 passed in 71.94s (0:01:11) |
| `fixture_freshness` | pass | 0 | 0.07s | fixture_freshness: OK — pricer-delta.diff still describes the fixture pair (78 lines) |

Tests: passed 674, collected 674
Test-id ledger: 674 ids · +35 / −0 (set-diff, not summary arithmetic)
Fix verification: 0 verified · 0 refused (cited test still fails at HEAD) · 3 measured but not verifiable
  - claims fix_verified the measurement does not show: VERDICT-F-36
  - 5 open finding(s) cite no test id, so the harness cannot fix-verify them
Diff coverage: 223/285 changed lines executed (78%) across 9 file(s); 50 test(s) touch the diff
  - 14 of them only in a subprocess the suite spawned — measured, but no test to name
  - tests/test_calibration.py: unexercised lines 494, 506, 514, 530, 538, 549, 561, 573
  - tests/test_fixture_freshness.py: unexercised lines 10-13, 15, 17, 20, 26-27, 29-34, 36-40, 42, 44-45, 48, 53, 67, 73, 77, 89, 100
  - tests/test_hooks.py: unexercised lines 553, 569, 579
  - tests/test_validate.py: unexercised lines 152, 161, 567, 577, 586, 595, 603, 611, 619, 626, 635, 644, 670, 680, 686, 709, 717, 725, 733, 744, 756

## Risks

**The measurement of the measurer failed silently, and I caught it by accident.** A scratch copy made exactly the way this project widened its own bash guard to permit — `cp -a <checkout> <scratch>` — carries `.venv`, whose editable-install `.pth` names the original checkout's `src` absolutely. Four defects injected into the scratch tree, four guard tests run, four greens, and every one of them would have been written into this state file as a fix-verified resolution. The tell was that *all four* passed: a fix-verification that never fails is not a verification. Re-run with PYTHONPATH pinned, all four bit. The four resolutions below are real; the procedure that produced the first attempt is filed as VERDICT-F-43 and is Major because it feeds `fix_verified`, which feeds the ledger, which feeds the published track record.

**The coverage gate's first live firing is a false alarm.** 91% → 78% reads as a 13-point regression and is not one: production changed-line coverage went 97% → 100% and the blended number fell because this range is 84% test code by line, and test files pay a structural unexercised tax the metric applies by design to production code. VERDICT-F-44.

**The bash guard has one more remove-what-you-read command.** 0.68.0 correctly fixed `mv` and `rsync --remove-source-files` and did not ask what else has that shape. `tar --remove-files -cf <scratch>/loot.tar <checkout>/hooks` is permitted at HEAD. VERDICT-F-42.

**Two findings are residuals of this range's own fixes** — the track record's new column (VERDICT-F-45) and the ledger's new digest (VERDICT-F-46) — and both are the same shape the fixes were about: the state is careful and the thing a human or an auditor reads is not.

Nothing in the range touched the eval corpus, the answer keys, the agent contract or the Action. The tamper anchors are intact and the fixture-freshness gate is now materially stronger than it was.

## Findings — REGRESSED first (10 open of 15 tracked)

### VERDICT-F-21 — STILL_OPEN — Major/P2 — age 1d

The run-history chain's anti-fabrication signal can still be shed by deleting data, and the gate then exits 0: 0.66.0 moved the ratchet into outcomes.json, but the anchor is itself unauthenticated, so removing one JSON key restores `unchained` with the whole track record intact
- UNTOUCHED IN THIS RANGE: `git diff --name-only 5dc59fb..a6011ec` lists neither src/verdict_mcp/gate.py nor tests/test_chain.py, and the only state.py change is the ledger digest (VERDICT-F-41). The mechanism the previous run measured across five gate arms stands unchanged.
- NO NEW EVIDENCE WAS GATHERED AND NONE WAS SOUGHT, stated so the age is not read as fresh confirmation. This finding is blocked on a maintainer decision, not on testing: is deleting `outcomes.json`'s `chain` key an accepted residual risk? Testing around a policy question would produce more measurement and no progress.
- THE SPECIFICATION TODAY IS THE TEST SUITE, which is what makes this a decision and not a bug report. tests/test_chain.py:162-179 asserts the arm-D shape is acceptable — `unchain(qa_root)` deletes the anchor by default — so changing the behaviour means changing those tests first. If the answer is "accepted", CHANGELOG.md:32-34 still states a price the deletion does not pay and should be corrected; if it is "not accepted", the ratchet has to leave the QA root, because no in-root anchor can distinguish a stripped project from a pre-upgrade one.
- The harness re-ran a chain test for this finding this run (`1 passed` at both 5dc59fb and HEAD). That is an inconclusive measurement about the delivered half of the fix and says nothing about the open half.

### VERDICT-F-39 — RESOLVED — Major/P1 — REAL_DEFECT

The 0.65.0 copier fix reads `mv` as a copy, so the strict bash guard now permits moving any file OUT of the checkout — including hooks/ itself — a shape it explicitly denied one commit earlier
- RE-INJECTION, and the guard test failed: tests/test_hooks.py::test_bash_blocks_moving_anything_out_of_the_checkout. In a scratch copy of the tree (never the checkout) I restored the v0.66.0 shape — `_COPIERS = {"cp", "mv", "install", "rsync", "ln"}` with `_MOVERS = set()` — and tests/test_hooks.py went from `148 passed in 5.44s` to `1 failed, 147 passed in 5.39s`, with `AssertionError: expected deny for: mv .../repo/src/app.py .../scratch/app.py, assert 0 == 2`. Restoring the file returned it to 148 passed. Hooks are invoked by absolute path, so this re-injection is unaffected by VERDICT-F-43.
- FIXED in d86ab07 (v0.68.0), hooks/enforce_bash_scope.py:47-53 and :367-412. `mv` left `_COPIERS` for a new `_MOVERS`; `_check_copier(head, args, moves=...)` now strips the destination out of `operands` and yields every remaining source as a target as well, and `rsync --remove-source-files` is treated as a move.
- MEASURED IN BOTH DIRECTIONS, as the previous run's focus item asked, feeding the hook its own PreToolUse payload with VERDICT_STRICT=1 and cwd=<checkout>. 13 of 13 must-deny probes DENY(2): `mv <checkout>/src/verdict_mcp/state.py <scratch>/stolen.py`, `mv -t <scratch> <checkout>/src/verdict_mcp/state.py`, `mv <checkout>/hooks <scratch>/hooks_gone`, `mv --target-directory=<scratch> <checkout>/hooks/qa_paths.py`, `mv -- <checkout>/... <scratch>/x.py`, `mv <checkout>/a.py <checkout>/b.py <scratch>/`, `rsync -a --remove-source-files <checkout>/src/ <scratch>/m/`, plus the eight into-the-checkout writes that must stay refused.
- AND THE OTHER DIRECTION IS INTACT — the fix did not restore the refusal by breaking the copier (VERDICT-F-37). 8 of 8 must-allow probes ALLOW(0): `cp -a <checkout> <scratch>/tree2`, single- and multi-source `cp` out, `cp -t <scratch> <checkout>/...`, `rsync -a <checkout>/src/ <scratch>/mirror/`, `ln -s <checkout>/... <scratch>/link.py`, `install -m 644 <checkout>/... <scratch>/a.py`, and `mv <scratch>/a.py <scratch>/b.py`.
- THE OVER-CORRECTION IS GUARDED TOO, which is the claim CHANGELOG.md:0.68.0 makes. Setting `_MOVERS = {"mv", "cp", "rsync", "ln", "install"}` in a second scratch tree fails exactly three tests (`3 failed, 145 passed`) — the two copy-out tests and the explicit-target-directory test — so both halves of the mv/copy distinction are pinned.
- THE SUITE COVERAGE GAP THE PREVIOUS RUN NAMED IS ONLY HALF CLOSED. tests/test_hooks.py:262-311, the parametrised denial table, still has an in-checkout destination on every entry (`mv a.py b.py`, `cp fix.py src/app.py`, `install -m 755 tool.sh bin/tool`, `ln -s a b`, `rsync a/ b/`), so the table alone would still not have seen this regression. The coverage arrived as three dedicated tests beside it instead, which is adequate for mv and rsync; `ln` and `install` are copiers and are correctly permitted outward, so they need no such row.

### VERDICT-F-36 — RESOLVED — Major/P2

The published track record still counts a claim-only confirmation as `held up` under a footnote that says the opposite: 0.64.0 split the counts in the state and in `reading`, but the report renderer prints neither
- RE-INJECTION, and the guard tests failed: tests/test_calibration.py::test_the_report_prints_how_many_of_the_confirmations_were_measured. In a scratch copy with PYTHONPATH pinned to the scratch `src` (see VERDICT-F-43), forcing `split = False` at harness.py:1793 took the pair of renderer tests from `2 passed` to `1 failed, 1 passed`; restoring returned `2 passed`. Restoring the old footnote wording independently took its own guard from `1 passed` to `1 failed` and back.
- FIXED in a6011ec (v0.70.0), src/verdict_mcp/harness.py:1786-1815 — the defect was in `_render_calibration`, not in the state block that 0.64.0 already fixed, which is what the previous run's focus item said to judge.
- JUDGED ON THE RENDERED OUTPUT, not the state. Calling `_render_calibration` on this project's own stored calibration prints `| Confidence claimed | Held up | of those, measured | Withdrawn | Rate |` with the row `| proven | 19 | 0 | 0 | _not yet_ |`, and the footnote now reads "a resolution nobody checked at all settles nothing" instead of "a finding merely resolved is not evidence either way". Both halves the finding named are in the artifact a human reads.
- A RESIDUAL IS FILED SEPARATELY rather than held under this id: the new column conflates a row written before `outcome_basis` existed with a row that was measured and found wanting. See VERDICT-F-45.

### VERDICT-F-26 — STILL_OPEN — Major/P1 — REAL_DEFECT

Fix verification picks its test by scraping any pytest node id out of a finding's prose evidence, so a mis-scraped id that fails at HEAD force-reopens a correctly-resolved finding over the tester's judgment
- UNTOUCHED IN THIS RANGE for the mechanism that is still open: `git diff 5dc59fb..a6011ec -- src/verdict_mcp/harness.py` changes only `_stamp_outcome` (VERDICT-F-40) and `_render_calibration` (VERDICT-F-36). `cited_tests` (harness.py:298-330), `select_test` (:333-348) and the `not_weighed` guard in `_apply_verification` (:558-566) are byte-identical to the versions the previous run cited.
- RE-PROVEN AT HEAD BY DIFFERENTIAL, exactly the two-real-ids probe the previous run's focus item specified. A resolved finding whose prose evidence names another finding's guard first and its own guard second, where BOTH ids landed this run so both are in `preferred`. Arm 1, both in `preferred`: harness.py:329 reorders `rest` stably, so prose order survives, the pick is the WRONG test, and `select_test` labels it `added_this_run`. `_apply_verification` then reads `arbitrary = selected_by == "first_cited" and candidates > 1` as False, so the `not_weighed` shield does not fire — and with `at_head == "fail"` the finding is force-reopened: status `open`, delta `STILL_OPEN`, `fix_verified` stripped, `resolution_refused` set naming a test that does not guard it.
- ARM 2, THE CONTROL, same finding and same pick with `preferred` empty: the label is `first_cited`, `not_weighed` fires, and the resolution stands. So the 0.65.0 shield is switched OFF by precisely the condition it exists for — a fix that ships with its own regression test, where every candidate is new.
- ARM 3: an explicit `verification_test` still wins. With it set, the candidate list is reordered, the pick is the finding's own guard and the label is `explicit`. That half of 0.65.0's fix holds, and it is the workaround this run used on every finding it resolved.
- THE HARNESS DID IT AGAIN THIS RUN, unprompted, on this project's own data: facts.json's `verification` block chose tests/test_calibration.py::test_the_tally_separates_what_was_measured_from_what_was_asserted for VERDICT-F-36 — the state-side guard from 0.64.0, not the renderer guard the finding was about. The measurement was pass/pass and settled nothing either way, so no harm landed; the selection was still of a third party.

### VERDICT-F-42 — NEW — Major/P2 — REAL_DEFECT

The strict bash guard's `tar` handler only inspects extraction, so `tar --remove-files -cf <scratch>/loot.tar <checkout>/hooks` is permitted — archive-then-delete is the same remove-what-you-read shape as the `mv` regression 0.68.0 just fixed
- hooks/enforce_bash_scope.py:309-318 `_check_tar` computes `extracting` from an `x` in the mode operand and returns immediately when it is False. Creating an archive is therefore never examined, and GNU tar's `--remove-files` deletes each source after adding it — the same class as `mv` and `rsync --remove-source-files`, both of which d86ab07 (v0.68.0) added to `_MOVERS` while leaving this one alone.
- PROVEN AT HEAD by feeding the hook its own PreToolUse payload with VERDICT_STRICT=1 and cwd=<checkout>: `tar --remove-files -cf <scratch>/loot.tar <checkout>/hooks` ALLOW(0), `tar --remove-files -cf <scratch>/loot.tar <checkout>/src/verdict_mcp/state.py` ALLOW(0), `tar -czf <scratch>/loot.tgz --remove-files <checkout>/hooks/enforce_bash_scope.py` ALLOW(0), and `gtar --remove-files -cf <scratch>/loot.tar <checkout>/hooks` ALLOW(0). The control in the same batch, `tar -xf <scratch>/evil.tar -C <checkout>`, is correctly DENY(2), so the handler runs and simply does not look at this half.
- THE IMPACT HALF IS DOCUMENTED, NOT EXECUTED, and I am not going to overstate it: this machine's bsdtar has no `--remove-files` (`tar --help` does not mention it), so the deletion could not be demonstrated here. GNU tar documents `--remove-files` as "remove files after adding them to the archive", and the project's CI matrix includes ubuntu, where tar is GNU tar. What is measured is the guard decision; what is inherited is the consequence.
- THIS IS THE CLASS LINK THE 0.68.0 FIX DID NOT COMPLETE. Searching the dispatcher at hooks/enforce_bash_scope.py:340-374 for every handler over a command that can remove what it read: `_MUTATORS` (rm/rmdir/unlink/shred/truncate) treat all operands as targets and are safe; `_check_find` covers `-delete` and `-exec <mutator>`; `_check_copier` now covers `mv` and `rsync --remove-source-files`; `_check_tar` covers extraction only. `tar` create-with-removal is the one remaining site, and it is the only one.
- SCOPE, stated because the hook's own docstring pre-empts part of it: hooks/enforce_bash_scope.py:10-17 says unknown commands are allowed and "a determined command can evade string analysis". `tar` is not an unknown command — it has a dedicated handler the project chose to write — so this is a gap inside declared scope, not the documented heuristic boundary.

### VERDICT-F-43 — NEW — Major/P2 — REAL_DEFECT

The re-injection procedure the agent contract mandates silently measures the unmodified original: a scratch copy made the way the guard was widened to allow carries `.venv`, whose editable-install `.pth` names the original checkout's `src` absolutely
- MEASURED DIFFERENTIAL, four re-injections run twice, identical in every respect but one environment variable. Copied the checkout to scratch with `cp -a`, injected four defects into the scratch `src/verdict_mcp/{harness,state}.py` (the renderer split, the renderer footnote, the `not_weighed` clause, the ledger digest), and ran each defect's guard tests. WITHOUT PYTHONPATH: 0 of 4 caught — every trial printed `intact 2 passed / injected 2 passed / restored 2 passed`. WITH `PYTHONPATH=<scratch>/src`: 4 of 4 caught, each `intact 2 passed / injected 1 failed, 1 passed / restored 2 passed`.
- THE MECHANISM, read and then confirmed by execution: `<scratch>/.venv/lib/python3.13/site-packages/_editable_impl_verdict_qa_mcp.pth` contains the ORIGINAL checkout's absolute `src` path. `python -c "import verdict_mcp.harness as h; print(h.__file__)"` run from the scratch tree with the scratch venv prints the original checkout's harness.py. With PYTHONPATH set, the same command prints the scratch tree's. sys.path order is what decides it: PYTHONPATH precedes the site-packages `.pth` additions.
- THE PRODUCT ENDORSES THE PROCEDURE THAT FAILS. hooks/enforce_bash_scope.py:40-46 records that `_COPIERS` exists precisely so `cp -a <checkout> <scratch>` is permitted — "the re-injection step the agent contract asks for" (VERDICT-F-37, fixed in 0.65.0). The agent contract's §3/§3.5/§6 mandate re-injection in a scratch copy and never name the isolation step. The harness's own verifier already solves it: facts.json's verification records `pythonpath: [<tmp>/verdict-verify-*/src, <tmp>/verdict-verify-*]`, so the machine-side implementation knows what the agent-facing prose does not.
- BLAST RADIUS: a false negative here reads as a green re-injection, which the contract converts into `fix_verified: true`, which `_stamp_outcome` converts into a `confirmed` ledger row with basis `claimed`, which the published Track record counts as `held up`. The failure mode is silent and self-flattering in exactly the direction §9 exists to prevent.
- WHAT I CANNOT ESTABLISH, said plainly: whether earlier runs of this project fell into the same trap. The state's evidence strings record the result of a re-injection and never the command, so runs 5-8's `fix_verified` claims cannot be re-derived from the record. Several of them quote specific non-trivial failure counts (`rc=1 | 2 failed, 2 passed`), which a no-op injection cannot produce, so at least those bit. The rest are unverifiable, and that unverifiability is itself the shape VERDICT-F-41 was about.
- SCOPE: only imports of the installed package are affected. `hooks/*.py` and `eval/fixture_freshness.py` are invoked by absolute path and were re-injected correctly without PYTHONPATH this run, which is why VERDICT-F-39's and VERDICT-F-13's verifications stand on the first attempt.

### VERDICT-F-44 — NEW — Major/P2

The diff-coverage percent blends test-file lines with production lines, and test-file lines carry a structural unexercised tax, so the run-over-run gate moves with the test/production composition of the diff rather than with coverage: 91% to 78% this run while production changed-line coverage rose from 97% to 100%
- MEASURED, same command as the previous run (`coverage_suite_cmd` from the profile front matter), harness-run: this run 223 of 285 changed measurable lines = 78%, against the previous run's 220 of 242 = 91%. Under §6 ("coverage on changed files must not decrease") that is a 13-point drop and a gate failure.
- SPLIT BY FILE KIND FROM THE HARNESS'S OWN `per_file` BLOCK, and the direction reverses. Production files this run — eval/fixture_freshness.py 7/7, hooks/enforce_bash_scope.py 10/10, src/verdict_mcp/harness.py 10/10, src/verdict_mcp/state.py 6/6, src/verdict_mcp/validate.py 1/1 — total 34 of 34 = 100%, against the previous run's 99 of 102 = 97%. Test files this run: 189 of 251 = 75%, against 121 of 140 = 86%. The blended number fell because this range is 477 changed lines of test code against 93 of production; the previous range was 140 against 102.
- THE MECHANISM IS DELIBERATE FOR PRODUCTION CODE AND WRONG FOR TEST CODE. src/verdict_mcp/harness.py:775-779: `executed = {ln for ln in ran if any(c for c in ctx.get(str(ln), []))}` — a line only counts if some dynamic context claims it, because "a `def` line runs at import under the empty context and proves nothing about the function it defines". `_changed_lines` (harness.py:610) applies no file filter, so test files go through the same rule: their imports, their `def test_*` lines, their decorators and — because coverage.py's `test_function` context only switches inside a function named `test_*` — every pytest fixture body are all counted unexercised, permanently and by construction.
- READ OFF THE DATA: tests/test_fixture_freshness.py is a brand-new file whose four tests all pass, and its unexercised ranges are 10-13, 15, 17, 20, 26-27, 29-34, 36-40, 42, 44-45, 48, 53 plus the four `def test_*` lines — i.e. the imports, the two module-level helper `def`s, and the whole body of the `fixture_repo` fixture, which runs on every one of the four tests. tests/test_validate.py contributes 21 such ranges, tests/test_calibration.py 8.
- CONSEQUENCE, which is why this is Major and not a curiosity: the previous run's focus item declared this gate "live in both directions" on the strength of two consecutive measured numbers. Its first live firing is a false alarm, and the shape recurs on every release whose diff is mostly tests — which, for a project whose releases are mutation-campaign test additions, is most of them. A gate that fires on composition trains its reader to wave it through, and the converse case is worse: a real production coverage drop can be masked by a large green test diff.
- PROPOSED VERIFICATION, since none exists: a test over `measure_diff_coverage` on a two-commit fixture repo whose diff adds one uncovered production line and forty lines of passing test code, asserting the reported production percent falls. Reporting production and test coverage as two numbers (or excluding `testpaths` from the denominator) is the design question behind it and is the maintainer's call.

### VERDICT-F-13 — RESOLVED — Minor/P2 — age 3d

fixture_freshness reproduces the fixture lossily: content of tracked files only, so mode changes, symlink swaps and planted untracked files are invisible, and a missing tracked file tracebacks
- RE-INJECTION, and three guard tests failed: tests/test_fixture_freshness.py::test_a_mode_change_is_no_longer_invisible. Reverting eval/fixture_freshness.py to its v0.66.0 content in a scratch tree took the new module from `4 passed in 2.13s` to `3 failed, 1 passed in 1.26s`; restoring returned `4 passed`. This file is a standalone script invoked by path, so the re-injection is unaffected by VERDICT-F-43.
- FIXED in a6011ec (v0.70.0), eval/fixture_freshness.py:56-70 and :76. `copy2(follow_symlinks=False)` replaces `copyfile`, a tracked file missing from the working tree now raises a named SystemExit instead of reaching `copyfile`, and `git ls-files --others --exclude-standard` refuses an untracked file planted under the fixture pair.
- THE FOURTH BLIND SPOT — a symlink swapped for a regular file — WAS PROBED BY HAND, because the suite does not cover it. Built a miniature fixture repo, anchored it green (`rc=0, OK, 8 lines`), replaced a tracked regular file with a symlink to identical content outside the tree: the gate now fails, `rc=1, regenerated: 16 lines`. The fix genuinely covers it; only the test is missing (VERDICT-F-47).
- THE GATE ITSELF PASSED THIS RUN, measured by the harness: exit 0 in 0.07s, "fixture_freshness: OK — pricer-delta.diff still describes the fixture pair (78 lines)".
- ONE EDGE REMAINS AND IS DELIBERATE, not filed as a defect: `--exclude-standard` means a planted file matching a .gitignore rule is still invisible. Probed and reproduced (`rc=0, OK` with `scratch_evil.py` planted under an ignore rule). The code comment states the trade-off and the alternative would re-open the `__pycache__` false alarm the whole design exists to avoid.

### VERDICT-F-22 — STILL_OPEN — Minor/P2 — age 1d

The VERDICT_STRICT bash guard reads shell metacharacters inside quoted strings and heredoc bodies as live syntax and resolves phantom targets against the checkout, blocking read-only QA commands
- UNTOUCHED IN THIS RANGE for this mechanism: the only change to hooks/enforce_bash_scope.py in 5dc59fb..a6011ec is the `_MOVERS` split and `_check_copier(moves=...)` (lines 43-53, 364-412). The redirection scan `_REDIRECT` and the segment splitter are byte-identical to the versions the previous run cited.
- RE-PROVEN FIVE TIMES THIS RUN, each on a read-only command, each costing a round trip, and every one of them inside a single-quoted heredoc where no redirection existed. (1) A probe script containing `len(sys.argv)` compared against 1 with a greater-than sign: the comparison inside the quoted Python body was read as an output redirection and refused with "output redirection targets '.../verdict-clone/1'". That was the first probe of the run.
- (2) A results-printing line containing the literal arrow written as a hyphen followed by a greater-than sign, refused with a target of the next token. (3) A survivor-classifying helper comparing a count against 2, refused with a target of `=`. (4) A JSON-patching script whose *string data* quoted one of this run's own findings, whose text in turn quoted a source line containing a comparison — refused with a target of `` 0` ``. The guard read a quotation of a quotation of source as live syntax.
- (5) THE SHARPEST ONE, and the reason this stays open rather than being written off as cosmetic: the follow-up script that tried to record reproductions 1 to 4 was itself refused, because the evidence text it carried contained the placeholder `[checkout]` written with angle brackets — the guard's own error-message format. Reporting the defect in the guard's own words is a command the guard refuses.
- THE COST IS REAL AND MEASURABLE: five of this run's Bash calls were refused and had to be rewritten to avoid greater-than and less-than characters anywhere inside quoted Python source. That meant writing comparisons as `not in (0, 1)`, spelling operators out in prose, and staging two finding texts through separate JSON files written with the Write tool rather than inline. Every one of the five was a read-only measurement.
- NOT EVERY REFUSAL THIS RUN WAS THIS DEFECT, and the distinction matters. A `mkdir` refused for an unresolved `$variable`, and an `rm .mutmut-cache` resolved against the hook's cwd rather than a preceding `cd`, are both the guard behaving exactly as designed and documented at hooks/enforce_bash_scope.py:22-26. Only the five above are the quoted-body misread.
- THE ASYMMETRY WORTH NOTING FOR THE FIX: the Write tool's guard (enforce_write_scope.py) permits only a .qa/ directory or the solo root, while the Bash guard additionally permits scratch under a temp dir. So the natural workaround for this defect — write the probe script with the Write tool instead of a heredoc — is itself refused, and the only remaining path is a heredoc that avoids the characters. Both guards are behaving as specified; the combination leaves no clean route.

### VERDICT-F-40 — RESOLVED — Minor/P2 — REAL_DEFECT

A measurement the harness itself records as "too weak to refuse a resolution" is still strong enough to deny that resolution its ledger row, and the ledger publishes a reason contradicting the harness's own note
- RE-INJECTION, and the guard test failed: tests/test_calibration.py::test_a_measurement_the_harness_declined_to_weigh_does_not_deny_the_row. In a scratch copy with PYTHONPATH pinned to the scratch `src`, rewriting the new clause at harness.py:1140-1141 back to an unconditional `and True` took the pair from `2 passed` to `1 failed, 1 passed`; restoring returned `2 passed`.
- FIXED in d86ab07 (v0.68.0), src/verdict_mcp/harness.py:1135-1141: `contradicts = attempted and v.get("at_head") == "fail" and not v.get("not_weighed")`. The two layers now read one record the same way.
- THE FALSE-POSITIVE HALF IS PINNED TOO: a deliberately chosen test that still fails at HEAD is a contradiction and still denies the row, guarded by its own test in the same module.

### VERDICT-F-41 — RESOLVED — Minor/P3

The permanent ledger records an outcome and a prose reason but not the measurement behind it, so a `confirmed` row cannot be audited once its finding leaves state.json — 19 of this project's 21 confirmed rows are already unjoinable
- RE-INJECTION, and the guard test failed: tests/test_calibration.py::test_the_ledger_row_keeps_the_measurement_not_only_the_sentence. Deleting the new `"verification": _verification_digest(finding),` line from state.py's `outcome_row` in a scratch copy with PYTHONPATH pinned took the pair from `2 passed` to `1 failed, 1 passed`; restoring returned `2 passed`.
- FIXED in d86ab07 (v0.68.0), src/verdict_mcp/state.py:222-231 and :249, and documented at docs/state-schema.md:325-329.
- THE FIX IS FORWARD-LOOKING ONLY, and this is worth stating so nobody reads the finding as retroactively repaired. Re-running the run-7 join over .qa/outcomes.json this run: 40 rows, 23 `confirmed`, 17 `unknown`; **0 of 40 carry a `verification` digest** (the field lands for the first time on rows written by this run), and only 2 of the 23 confirmed rows still have a finding in state.json to join to. The 21 historical unjoinable confirmations stay unjoinable.
- A RESIDUAL IS FILED SEPARATELY: the digest keeps four fields and drops the two that explain why a failing measurement did not count. See VERDICT-F-46.

### VERDICT-F-45 — NEW — Minor/P2

The Track record's new `of those, measured` column counts a row written before `outcome_basis` existed as not-measured, under a footnote asserting the two kinds are exhaustive — the state refuses that relabelling and the renderer performs it
- RENDERED AT HEAD from this project's own stored calibration, by calling `_render_calibration` directly: `| proven | 19 | 0 | 0 | _not yet_ |` under `| Held up | of those, measured | Withdrawn | Rate |`, followed by "`held up` covers two things and they are not equal: a re-injection the harness measured, and one the tester asserted where no measurement contradicts it. The `measured` column is the first kind. Read them apart." A reader subtracts 19 minus 0 and concludes 19 were the tester's word. The stored bucket is `confirmed 19, confirmed_measured 0, confirmed_claimed 2` — 17 rows carry no basis at all.
- THE STATE IS CAREFUL WHERE THE RENDERER IS NOT, which is the same split-brain shape as VERDICT-F-36 itself. src/verdict_mcp/state.py:350-356 comments "Rows written before `outcome_basis` existed have neither, and calling them the tester's word would be a claim of its own. The split is stated only for what carries one" — and that careful `reading` string is built only in the `decided >= min_sample` branch. Below the sample threshold, which is where this project sits (19 of the needed 30), `reading` carries no split at all and the rendered table is the only thing anyone sees.
- THE GUARD TEST CANNOT SEE IT. tests/test_calibration.py's `_rows_for_report(measured, claimed, refuted)` helper builds every confirmed row with an explicit `outcome_basis`, so `confirmed == measured + claimed` holds in all three new renderer tests by construction (2+5=7, 1+1=2, 3+0=3). The one shape the project's own data has — `confirmed > measured + claimed` — is unrepresentable in the fixture.
- A SECOND, SMALLER READING ERROR IN THE SAME TABLE: `split` is decided globally (harness.py:1793, `any(v.get("confirmed_claimed") for _, v in rows)`), so the `unstated` and `probable` rows print `0` in the measured column although neither has a single basis-carrying row. `0` there means "no data", not "none measured".
- DIRECTION OF THE ERROR, stated because it changes the priority: this under-claims rather than over-claims — it makes the tester look less measured than the record supports. That is the safe direction, which is why it is Minor and not Major. It is still a number that cannot be added up.

### VERDICT-F-46 — NEW — Minor/P3

The new permanent-ledger verification digest drops `not_weighed` and `candidates`, so a confirmed row that stands over a measurement the harness declined to weigh is recorded forever as a confirmation beside a test that failed at HEAD, with nothing saying why it did not count
- src/verdict_mcp/state.py:222-229 `_verification_digest` keeps exactly `("test", "at_previous", "at_head", "selected_by")`. `not_weighed` — the note `_apply_verification` writes at harness.py:559-563 when it declines to let a prose-order pick refuse a resolution — and `candidates`, without which `selected_by: first_cited` cannot be interpreted, are both dropped.
- PROVEN BY EXECUTION against the shipped `outcome_row`: given a finding with `outcome: confirmed`, `outcome_basis: claimed` and a verification record carrying `selected_by: first_cited`, `candidates: 4`, `at_head: fail` and a `not_weighed` note, the row written is `{'test': ..., 'at_previous': 'pass', 'at_head': 'fail', 'selected_by': 'first_cited'}`. A future auditor joining that row sees a confirmation standing beside a test that failed at HEAD and no explanation — which is precisely the reading VERDICT-F-40 was filed to stop, preserved permanently by the fix for VERDICT-F-41.
- SCOPE AND COST: two more keys on a hundred-byte row, and only on rows that carry a declined measurement. The proposed verification is an assertion in the existing ledger-digest test that a `not_weighed` record survives the round trip.
- CONFIDENCE NOTE: this is a real but narrow record-fidelity defect, and it can only bite a reader who joins the ledger long after the fact. Filed Minor/P3 deliberately rather than attached to VERDICT-F-41, because that finding is genuinely fixed and this is a distinct claim.

### VERDICT-F-47 — NEW — Minor/P3

The fixture-freshness gate's symlink blind spot is fixed but untested, while CHANGELOG.md claims "four now, one per blind spot" — three of the four new tests cover blind spots and the fourth is a green-path control
- The four tests in tests/test_fixture_freshness.py are the green-pair control, the mode change, the planted untracked file, and the missing tracked file. The four blind spots CHANGELOG.md names for 0.70.0 are the mode change, the symlink swap, the planted untracked file, and the missing tracked file. The symlink swap — named in the fix's own docstring at eval/fixture_freshness.py:45-49 — has no test.
- THE BEHAVIOUR ITSELF IS SOUND, which is why this is a test gap and not a defect. Probed by hand on a miniature fixture repo: anchored green at `rc=0, 8 lines`, then replaced a tracked regular file with a symlink to identical content outside the tree — `rc=1, regenerated: 16 lines`. `copy2(follow_symlinks=False)` carries the link and `git diff --no-index` reports the type change.
- WHY IT MATTERS AT ALL: this gate is a tamper-evidence anchor for the eval answer keys, and the symlink swap is the one blind spot of the four that an adversary rather than an accident would use. An untested guarantee on a trust anchor is the kind that quietly stops holding, which is the pesticide-paradox argument for pinning it now while the fix is fresh.
- PROPOSED VERIFICATION: a fifth test alongside the existing three, building the same miniature repo, swapping a tracked file for a symlink with identical bytes, and asserting a non-zero exit — and failing when `follow_symlinks=False` is dropped from eval/fixture_freshness.py:76.

### VERDICT-F-48 — NEW — Minor/P2

Five non-equivalent operator and guard mutants in validate.py survive the whole 674-test suite — including the judgment validator's status-enum check and both of the PostToolUse hook's exit codes — while the profile records that only message text and two accepted-equivalent clock mutants remain
- MEASURED THIS RUN, instrument controlled first as the profile requires. A fresh mutmut campaign over src/verdict_mcp/validate.py in a scratch tree, runner `tests/test_validate.py`: 574 mutants, 391 killed / 183 survived. Of the 183 survivors, 9 sit on lines with no string literal to rewrite. Each of those 9 was then applied one at a time and re-checked against the WHOLE suite — the method the previous campaign used and the only honest way to read a scoped run.
- TWO OF THE NINE ARE SCOPED-RUNNER ARTIFACTS and die against the whole suite: the changed-lines guard at validate.py:278 mutated to admit a zero-line diff (`1 failed, 673 passed`) and the flaky_quarantine type guard at validate.py:519 mutated to its negation (`53 failed, 621 passed`). Two more are the clock tolerances the profile already accepts as equivalent — both comparison operators at validate.py:396 and :398 relaxed by one second — and both survive at `674 passed`, as documented.
- FIVE SURVIVE THE WHOLE SUITE AND ARE NOT EQUIVALENT. Each was applied alone and produced `674 passed in ~74s`, with `674 passed` again after restoring; the scratch baseline was `674 passed in 74.02s`. (a) `status = _status(f)` rewritten to `status = None` inside `validate_judgment`, so the status-enum refusal never fires — and no test in the 0.67.0 batch covers `status`, while severity, priority, failure_classification, fix_verified, duplicate ids, index naming, every-finding and the pass-cap all have one. (b) and (c) two `continue` statements in the finding loops rewritten to `break`, which stops checking after the first malformed entry; the every-finding test passes well-formed dicts and so cannot reach the non-dict `continue`. (d) and (e) `_hook_mode`'s two exit codes, at validate.py:551 (an unreadable state.json, exit 2) and validate.py:556 (a clean state, exit 0), each rewritten to a different integer with nothing failing. That hook is what the agent contract calls binding.
- THE PROFILE'S SUMMARY IS UNDERSTATED IN THE DIRECTION THAT MATTERS. .qa/profile.md:82-85 records "What remains is mostly message-text and two `>` to `>=` mutants at the clock tolerances, which differ by one second and are accepted as equivalent." Measured: five more survive that are neither message text nor equivalent. The campaign's headline improvement is real and larger than claimed — 335 killed to 391 killed, +56, same tool and scope — and the residual is smaller than it should be only in the profile's telling.
- PROPOSED VERIFICATION, one test per gap and no source change: a judgment finding carrying an invented `status` must be refused; a judgment whose findings list holds a non-dict entry followed by a bad dict must still name the bad dict; and two hook-mode tests feeding a PostToolUse payload — one over an unreadable state.json asserting exit 2, one over a clean state.json asserting exit 0 with empty stderr.
- SCOPE, stated so this is not read as a live defect: nothing in the shipped behaviour is wrong today. What is measured is that five guards could be broken without a test noticing, in the one module written to refuse what the model writes.


## Track record

47 findings tracked across this project's history · 28 settled, 19 still undecided.

| Confidence claimed | Held up | of those, measured | Withdrawn | Rate |
|---|---|---|---|---|
| proven | 24 | 0 | 0 | _not yet_ |
| unstated | 3 | 0 | 0 | _not yet_ |
| probable | 1 | 0 | 0 | _not yet_ |

*A rate appears once a row has 30 settled outcomes. Settled means fix-verified or regressed (it held up) against withdrawn (it did not); a resolution nobody checked at all settles nothing.*

*`held up` covers two things and they are not equal: a re-injection the harness measured, and one the tester asserted where no measurement contradicts it. The `measured` column is the first kind. Read them apart.*

## Release blockers

_None._

## Verified intact

- The direction the bash guard exists to protect is unbroken, and the fix for VERDICT-F-39 did not buy it by breaking the copier. 13 of 13 write-into-the-checkout probes DENY(2) at HEAD (`cp -a`, multi-source `cp`, `cp -t`, `rsync`, `ln -sf`, `install -m 644`, `rm -rf`, `tar -x -C`, and six `mv` forms), and 8 of 8 read-out-to-scratch probes ALLOW(0). Both directions measured in one batch against the shipped hook.
- Suite green and growing, measured by the harness: 674 passed, exit 0, 72.17s. Test-id set-diff is +35/−0 — no test was removed, silently or otherwise — and the count-drop gate could fire because the pytest dialect was recognised.
- Suite duration is inside the 10% week-over-week gate and moved the right way: 72.2s against the previous run's 75.8s, −4.8%.
- Production changed-line coverage rose: 34 of 34 changed measurable production lines executed (100%) against 99 of 102 (97%) last run, same command, status `measured`. The headline blended number fell for a reason that is not coverage (VERDICT-F-44).
- The fixture-freshness gate passed, exit 0 in 0.07s, "pricer-delta.diff still describes the fixture pair (78 lines)" — and its four blind spots were re-probed by hand, three by re-injection and the fourth (symlink swap) directly.
- Tamper-evidence anchors untouched by the range: `git diff --name-only 5dc59fb..a6011ec -- eval/fixtures/ agents/ standards/ templates/ action.yml commands/` prints nothing. No answer key, no corpus file, no fixture and no agent contract moved.
- The mutation instrument is sound, which the profile required before any mutation number is quoted. Applying mutant 67 — a guard inversion the tool reports as KILLED — to the scratch tree took the whole suite from `674 passed` to `79 failed, 595 passed`; restoring returned it to green. Survivals in this campaign are therefore real survivals, not a broken runner.
- Suite quality against validate.py improved measurably across 0.67.0 and 0.69.0, and the number is this run's own rather than the profile's: a fresh mutmut campaign over src/verdict_mcp/validate.py, same scope and same runner as the previous one, reports 574 mutants with 391 killed / 183 survived, against 335 killed / 239 survived before that work. +56 mutants now killed. The residual is larger than the profile records, which is VERDICT-F-48.
- Version metadata agrees across the three files that carry it: pyproject.toml:8, server.json:10 and :15, and .claude-plugin/plugin.json:3 all read 0.70.0.
- The explicit-citation half of VERDICT-F-26's 0.65.0 fix holds: an explicit `verification_test` still outranks prose order and is labelled `explicit`. It is the workaround this run used on all five findings it resolved, and it is why those five verifications can be trusted.
- The code census over the range is clean. No placeholder or swallowed-exception hits in production code (the four `for_now` hits are all inside .qa/ prose written by the previous run), and the single undeclared-import lead — `pytest` at tests/test_fixture_freshness.py:15 — is a dev-group dependency declared at pyproject.toml:84, not a hallucinated package. 0 of 5 commits in the range carry AI-authorship trailers.

## Not tested

- Windows and the rest of the CI matrix (ubuntu/windows x py3.10/py3.13). This machine is macOS + py3.13 only. Directly relevant this run: VERDICT-F-42's impact half depends on GNU tar, which this machine does not have, and `_check_copier`'s new `moves` path does path arithmetic that was never exercised on Windows separators.
- The eval corpus, the scorer and the answer keys. `git diff --name-only 5dc59fb..a6011ec -- eval/fixtures/ agents/ standards/ templates/ action.yml commands/` prints nothing, so agent-prompt regressions — which are only visible through the corpus, not the unit suite — were not re-measured. eval/fixture_freshness.py is the one file under eval/ that changed and it was tested.
- agents/verdict.md itself, untouched by the range and not re-read against the corpus — notable because VERDICT-F-43 is a defect in what that file instructs, and the fix belongs there.
- The Action's gate mode as GitHub executes it, and the Action's run mode, which the profile forbids locally.
- A mutation campaign over anything but src/verdict_mcp/validate.py. That one file was re-measured this run (574 mutants) with the instrument controlled first, and all nine non-string survivors were individually re-checked against the whole suite.
- The other five modules in src/verdict_mcp/ and both hooks remain mutation-unmeasured. src/verdict_mcp/harness.py is this project's top defect cluster (21.0 severity-weighted, 9 findings, 7 still open) and has never had a campaign run against it.
- server.py, runner.py, census.py, project_key.py, issues.py and profile.py were not read this run beyond `_render_calibration`'s consumer; none is in the range.
- VERDICT-F-21's open half was deliberately not re-probed — it is blocked on a maintainer decision, and further measurement would add evidence to a question that is not empirical.
- Whether runs 5-8's `fix_verified` claims were produced by a correctly isolated re-injection (VERDICT-F-43). The record does not carry the command, so this is unverifiable rather than untested, and no amount of re-reading state.json will settle it.
- Concurrency: two verdict-facts/verdict-finalize runs against the same QA root at once. The run-number collision check was read, not exercised.
- `tar`, `install` and `ln` shell forms beyond the probes listed in VERDICT-F-39 and VERDICT-F-42 — in particular globs, `--` separators, and quoted operands containing spaces.

## Fix order

Dependencies first, then severity. VERDICT-F-43 leads because it changes how every other fix on this list gets verified, and because until it is closed no `fix_verified` in this project can be taken at face value.

1. **VERDICT-F-43** — name the isolation step in `agents/verdict.md` §3/§6 and in `.qa/lessons.md`, and consider a harness helper that builds a scratch tree the right way (the verifier already knows how). Everything below is verified by re-injection, so this comes first.
2. **VERDICT-F-42** — give `_check_tar` a create-with-removal arm. Small, self-contained, and it closes the class link 0.68.0 left open in risk area 1. It does not depend on 1, but it should be *verified* after 1.
3. **VERDICT-F-44** — decide what the diff-coverage number means before changing it. This blocks the §6 coverage gate from being trustworthy in either direction, and the longer it stands the more it trains its reader to skip the line.
4. **VERDICT-F-26** — the `preferred` reorder at harness.py:329. Now the oldest open Major, and it is actively selecting third-party tests on this project's live data every run.
5. **VERDICT-F-45** — one column and one footnote in `_render_calibration`, plus a fixture that can represent `confirmed > measured + claimed`. Cheap, and it finishes 0.70.0's own headline.
6. **VERDICT-F-46** — two keys in `_verification_digest`. Cheapest item here; do it in the same commit as 5 if the ledger is open anyway.
7. **VERDICT-F-47** — the fifth fixture-freshness test. Pin the symlink behaviour while the fix is fresh.
8. **VERDICT-F-48** - four or five tests, no source change. The cheapest way to finish the campaign 0.67.0 and 0.69.0 started, and it corrects the profile line that understates the residual.
9. **VERDICT-F-22** — the quoted-body misread. Unchanged in severity but it cost five round trips this run; it is a tax on every future run.
10. **VERDICT-F-21** — not a fix, a decision. It cannot be scheduled until the maintainer answers.

## Next run focus

- VERDICT-F-43 first, because it decides how much of this project's own track record is worth. Two things to settle: whether `agents/verdict.md` and lessons.md now name the isolation step, and whether the harness grew a helper that makes a correctly isolated scratch tree (the verifier at harness.py already sets PYTHONPATH — the agent-facing path is the one missing it). Verify by repeating this run's differential: four injections, with and without PYTHONPATH, expecting 4 of 4 caught either way once a helper exists.
- VERDICT-F-42: check `_check_tar` grew a create-with-removal arm, and probe `tar --remove-files -cf <scratch>/x.tar <checkout>/hooks` in both directions — it must DENY while `tar -cf <scratch>/x.tar <checkout>/hooks` without the flag must still ALLOW, since archiving out is a read.
- VERDICT-F-44 is a design decision before it is a fix: does the diff-coverage percent exclude `testpaths`, report production and test as two numbers, or stay blended with the gate reading only the production half? Whatever is chosen, the run-over-run comparison in §6 must be made against a number that does not move with diff composition. Re-measure against this run's 78% blended / 100% production.
- VERDICT-F-26's residual is unchanged and is now the oldest open Major. The fix is in the `preferred` reorder at harness.py:329, which is a no-op when every candidate is preferred; the two-real-ids probe with both ids in `added` is the regression test, and the `not_weighed` shield should key on "the pick was not the tester's own citation" rather than on the `first_cited` label.
- VERDICT-F-21 needs the maintainer's answer, not another measurement: is deleting `outcomes.json`'s `chain` key an accepted residual risk? If yes, close it risk-accepted and correct CHANGELOG.md:32-34, which states a price the deletion does not pay. If no, the ratchet must leave the QA root. tests/test_chain.py:162-179 is the specification today and changes first either way.
- Re-run the mutmut campaign over src/verdict_mcp/validate.py and compare against this run's measured 574 mutants / 391 killed / 183 survived (runner `tests/test_validate.py`). Control the instrument first as the profile requires; mutant 67 is a known-good control. Expect VERDICT-F-48's five whole-suite survivors to be killed. Then consider the first campaign over src/verdict_mcp/harness.py, this project's top defect cluster, which has never been mutation-measured.
- Keep setting `verification_test` on every finding this project resolves. All five resolutions this run carry one; the harness still chose a third-party test for VERDICT-F-36 during facts, which is VERDICT-F-26 acting on live data and is the reason the explicit citation is not optional.
- Re-read `.qa/outcomes.json` and join every `confirmed` row against its `verification` digest. This run's rows are the first that can be joined at all (0 of 40 carried one before); by next run there should be five, and each must show `at_previous: fail` and `at_head: pass` or say why not.

## Notes

**On the mutation campaign.** The profile required the instrument be controlled before any number is quoted, and it was: mutant 67, a guard inversion mutmut reports as KILLED, takes the whole suite from 674 passed to `79 failed, 595 passed` when applied by hand, and restoring returns it to green. Survivals in this campaign are real survivals.

The campaign was **re-run this session rather than inherited** — 574 mutants over `src/verdict_mcp/validate.py`, runner `tests/test_validate.py`, in a scratch tree — and measures **391 killed / 183 survived**, against the previous campaign's 335 / 239. Same tool, same file, same runner shape: **+56 mutants now killed** across 0.67.0 and 0.69.0. That improvement is measured, not inherited.

All nine non-string survivors were then applied one at a time and re-checked against the whole 674-test suite. Two die there and were scoped-runner artifacts. Two are the clock tolerances already accepted as equivalent. **Five survive and are not equivalent**, and they are filed as VERDICT-F-48: the judgment validator's status-enum check, two `continue`-to-`break` mutants in the finding loops, and both of `_hook_mode`'s exit codes.

One number in the profile is **inherited, not verified**, and must not be read as though it were: the split of survivors into "186 message-string / 53 operator-boundary-guard", and the claim that 23 of those 53 are now killed. My classifier is line-based and coarser — it calls a survivor message-text if its source line is quote-heavy — and reports 174 message-text against 9 operator/boundary. That is a different measurement and cannot be subtracted from the profile's. The 391 / 183 totals are this run's; the 53 / 23 figures are the previous run's and were not re-derived.

**On what this run did not do.** It did not re-derive VERDICT-F-21 (a policy question, not an empirical one), did not touch the eval corpus (unchanged in the range), and did not run a mutation campaign over `harness.py`, which is where the defects actually cluster. That is the biggest unmeasured surface in this project and it has been available for three runs.

**On honesty about my own instruments.** Four Bash calls this run were refused by the project's own guard for metacharacters inside quoted Python bodies (VERDICT-F-22), and one re-injection batch measured the wrong tree entirely (VERDICT-F-43). Both are recorded rather than smoothed over: a QA run that hides its own false starts is asking to be believed on the rest.

---

*Countable sections rendered from `state.json` by `verdict-finalize`; the prose is the agent's. They cannot disagree.*
