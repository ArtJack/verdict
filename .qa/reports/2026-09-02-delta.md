# QA report — verdict · run 5 (delta)

**VERDICT: pass with risks**

## Scope

- Range: `c38631793efefa1fda4e8f1453bb9318e8c772f6..32d1c413b80b766ea2287f8753f9cab326120d8e` · 34 files changed, 3019 insertions(+), 328 deletions(-)
- Branch: `main` · measured 2026-09-02T15:38:59Z
- Isolation check: **pass**

Delta run over c386317..32d1c41 - 9 commits, 34 files, 3019 insertions / 328 deletions, releases v0.51.0 through v0.56.0. Team mode; the QA root is the committed .qa/ and it is the only system of record consulted. Project key 'verdict' from the profile header, which is itself the subject of resolved finding VERDICT-F-23. The previous run's next_run_focus was addressed in the order it asked for: F-23 first, then F-24, then F-20.

This is a delta, not a re-baseline: 34 files and 3347 changed lines are inside the ~100-file / ~10,000-line thresholds, and the previous run is 11 hours old.

What this run did NOT do is as important as what it did. Six of the nine open findings inherited from run 4 were fixed in this range, and the work was to verify those fixes rather than accept them - five of the six are fix-verified by counterfactual or re-injection, each cited on the finding. The remaining budget went to the code that shipped the fixes, which is where all five new findings are.

## Gates

| Gate | Result | Exit | Duration | Summary |
|---|---|---|---|---|
| `suite` | pass | 0 | 58.23s | 593 passed in 57.95s |
| `fixture_freshness` | pass | 0 | 0.06s | fixture_freshness: OK — pricer-delta.diff still describes the fixture pair (78 lines) |

Tests: passed 593, collected 593
Test-id ledger: 593 ids · +51 / −1 (set-diff, not summary arithmetic) — id lists truncated to 50 each
Fix verification: 1 verified · 0 refused (cited test still fails at HEAD) · 0 measured but not verifiable
  - 8 open finding(s) cite no test id, so the harness cannot fix-verify them
Diff coverage: 705/1125 changed lines executed (63%) across 19 file(s); 50 test(s) touch the diff
  - hooks/enforce_run_contract.py: unexercised lines 66, 105-112, 116-127, 130 (not imported by anything the suite executed)
  - hooks/report_open_findings.py: unexercised lines 105-109 (not imported by anything the suite executed)
  - src/verdict_mcp/gate.py: unexercised lines 127 — functions never entered: evaluate
  - src/verdict_mcp/harness.py: unexercised lines 40-41, 44, 262-264, 267, 283, 293, 297, 311-314, 317, 326-329, 335, 347, 352, 377, 384, 393, 429, 478-480, 483, 500, 513, 516, 528, 538, 572-573, 580-582, 586, 592-593, 1241, 1261, 1273, 1282, 1290, 1297, 1328, 1332, 1378-1383, 1420, 1501-1502, 1506, 1625 — functions never entered: facts_main, finalize_main
  - src/verdict_mcp/issues.py: unexercised lines 1-217 (not imported by anything the suite executed)
  - src/verdict_mcp/profile.py: unexercised lines 46
  - src/verdict_mcp/state.py: unexercised lines 648, 766, 769
  - src/verdict_mcp/validate.py: unexercised lines 267
  - tests/test_artifact_check.py: unexercised lines 9-12, 14, 16, 18, 21, 27, 31, 43, 53, 63, 70, 77
  - tests/test_code_drift.py: unexercised lines 231, 238, 247
  - tests/test_diff_coverage.py: unexercised lines 7-11, 13, 15, 17, 19, 22, 27, 33-34, 37, 46, 58, 88, 96, 111, 120, 133, 141, 154, 168, 177
  - tests/test_gate.py: unexercised lines 514
  - tests/test_harness.py: unexercised lines 823, 841, 851, 861, 868
  - tests/test_issues.py: unexercised lines 8-13, 15, 18, 33, 40, 53, 57-58, 66, 78, 83, 87, 91, 102, 116, 124, 137, 146, 153, 159
  - tests/test_stop_hook.py: unexercised lines 179, 189
  - tests/test_validate.py: unexercised lines 437, 453
  - tests/test_verification.py: unexercised lines 9-12, 14-16, 18, 20, 23, 28, 34-36, 39, 49, 63, 72, 91, 109, 129, 147, 167, 187, 209, 218, 232, 244, 255, 261

## Risks

**The instrument is measuring itself, and this run found three defects in the newest measurement code.** v0.53.0-v0.56.0 added fix verification, diff coverage and the issue-tracker bridge - roughly 1000 new lines in harness.py and issues.py - and VERDICT-F-25, F-26 and F-28 are all in that code. The pattern is consistent and worth naming: each takes a coarse proxy for a precise object (an evidence string for a test function, a file path for a test function, an in-process trace for 'what the suite executed') and then reports the proxy's answer as measured fact. Verdict's whole claim is 'measured, not composed'; a measurement that is silently wrong is worse for that claim than one that is absent, because the absent one is reported as unmeasurable.

**VERDICT-F-26 is the one to fix first**, and it is worse than its severity reads on its own: it lets a mis-parsed string overturn a tester's explicit `resolved` and write STILL_OPEN into the system of record, and that wrong status then flows into outcomes.json - the calibration ledger that §9 says the model must not be able to influence. It is capped at Major rather than Critical because it fails toward holding findings OPEN, which is the safe direction, and because no wrong STILL_OPEN is currently in this repository's state. It becomes Critical the moment one is found there.

**VERDICT-F-29 is the most irreversible.** A 95 MB coverage.json is sitting untracked in a directory the maintainer is expected to commit. A .gitignore line costs nothing today; after the commit, the blob is in the history permanently and 5% under GitHub's 100 MB push wall.

**Carried risk.** VERDICT-F-21 is now the oldest open Major and is unchanged: deleting runs.jsonl still restores the chain_intact signal, so the anti-fabrication ratchet is defeated by deletion rather than forgery. The live half improved - this repository's own ledger is chained and verifies - so the exposure is future, not present.

**Not a risk, and worth saying because a count suggests otherwise:** src/verdict_mcp/issues.py reads as 0% covered in this run's own coverage numbers. It is not untested; it has 9 tests that run it as a subprocess. That is VERDICT-F-28, and it is the reason no finding was filed about issues.py being unexercised.

## Findings — REGRESSED first (9 open of 15 tracked)

### VERDICT-F-21 — STILL_OPEN — Major/P2 — age 1d

The run-history chain has no cross-file ratchet: with runs.jsonl absent or unchained, --require-harness reverts to the imitation-defeatable state and exits 0 - and this repository's own ledger is currently unchained
- RE-PROVEN AT HEAD 32d1c41 by differential probe, in a scratch copy of .qa/ (tempfile dir, never the checkout). With the ledger present: verify_chain -> 'intact', harness_signals -> chain_intact True. With runs.jsonl unlinked and last_run.chain removed from the copied state: verify_chain -> 'unchained', harness_signals -> chain_intact True again, missing_durable -> []. Deleting the evidence restores the signal.
- Unchanged in this range: `git log --oneline c386317..HEAD -- src/verdict_mcp/state.py` shows the only state.py commits touched code_drift and project_key_for_root; _chain_signal at state.py:469-477 and DURABLE_SIGNALS at state.py:421 are byte-identical to the run-4 reading.
- src/verdict_mcp/state.py:475-477 is still the carve-out: `if result["status"] == "unchained": return not recorded` - a state that simply omits last_run.chain satisfies the strongest of the three durable signals by claiming nothing.
- src/verdict_mcp/gate.py:173-174 still records the unchained case as advisory only and never touches exit_code.
- PARTIAL PROGRESS, recorded honestly: the live-exposure half of this finding is closed for the rows that exist. .qa/runs.jsonl now carries chained rows from run 4 onward and verify_chain over the real ledger returns 'intact', so the VERDICT-F-12 hardening does protect this repository today - against forgery. The evasion is deletion, and deletion is still free.

### VERDICT-F-23 — RESOLVED — Major/P2 — age 1d

The harness derives project identity from the git directory basename and never reads the profile's Project-Key header, so a clone under a different directory name silently re-keys a committed team-mode state and its run ledger
- FIXED at src/verdict_mcp/state.py:766-782 - new project_key_for_root(root) reads `Project-Key:` from the QA root's profile.md, in both the bold and bare spellings, and returns None rather than raising when the profile is unreadable.
- Wired at src/verdict_mcp/harness.py:683-689 with the §0 precedence: the profile's recorded key wins over the derived one, and a previous state's key is the second fallback - `recorded = project_key_for_root(qa_root); if recorded and recorded != key: key, key_source = recorded, "profile"`.
- FIX-VERIFIED by counterfactual on the same checkout: `derive_key(repo)` still returns ('verdict-clone', 'git') here - the wrong key is still what the directory says - while `project_key_for_root(.qa)` returns 'verdict'. At the previous commit, `git show c386317:src/verdict_mcp/harness.py | grep -n derive_key` shows line 278 `key, key_source = derive_key(repo)` as the ONLY identity source, so the old code had no path to the right answer on this checkout.
- LIVE: this run's facts.json records project 'verdict' with project_key_source 'profile'. Run 4's state on the same checkout recorded 'verdict-clone'. Same repository, same directory name, different answer - the fix is what changed.
- Belt and braces, also new in this range: harness.py:1287-1289 check_artifacts re-reads the INDEX after finalize and reports 'INDEX row names project X, the state says Y', so a future re-keying is caught in the artifact rather than three runs later.
- Residual, not a defect and stated for the record: the INDEX rows for runs 1-3 read 'verdict', run 4 reads 'verdict-clone'. finalize does not rewrite history, so the split row stands as evidence. Repairing it is a maintainer's edit, not the harness's.

### VERDICT-F-20 — RESOLVED — Major/P2 — REAL_DEFECT — age 1d

The test-id set-diff truncates added/removed to 50 and then reports the truncated list length as the delta, so a mass test deletion is rendered as '-50' under a line claiming 'set-diff, not summary arithmetic'
- FIXED at src/verdict_mcp/harness.py:864-868 - the counts are now taken from the untruncated sets and only the lists are capped: `"added_count": len(added), "removed_count": len(removed), "added": added[:50], "removed": removed[:50], "truncated": len(added) > 50 or len(removed) > 50`.
- Both renderers now read the counts: harness.py:1609-1613 (report) uses added_count/removed_count with a `- id lists truncated to 50 each` suffix when the cap engaged, and harness.py:1153-1155 (INDEX row) does the same.
- FIX-VERIFIED by counterfactual on the run-4 scenario, old renderer vs new renderer over identical measured input {count 20, added_count 0, removed_count 180, removed capped to 50 entries, truncated true}: OLD renders 'Test-id ledger: 20 ids . +0 / -50', NEW renders 'Test-id ledger: 20 ids . +0 / -180'. The mass deletion that run 4 would have reported as -50 now reports -180.
- LIVE RECONCILIATION, which is what failed at run 4: this run measures 593 ids, +51 / -1 against the 543-id stored ledger, and 543 + 51 - 1 = 593 closes exactly. Run 4 reported +50 against a 377-id ledger and a 543-id suite, which did not close.
- The one removed id is accounted for and is not a silent drop: tests/test_validate.py::test_a_run_with_no_gates_claims_no_suite_and_is_left_alone was deliberately inverted by the VERDICT-F-17 fix and replaced by two tests in the same file.
- Residual noted, not blocking: the cap is still untested - no test in tests/test_harness.py constructs a diff larger than 50, so the `truncated` flag and the suffix have no guard. Reported as a gap, not re-filed as a finding.

### VERDICT-F-17 — RESOLVED — Major/P2 — age 1d

_unmeasured_suite early-returns on empty gates, so an unqualified `pass` stands over a run that measured nothing - and the harness drops the `no_gates` fact at the merge boundary, so state cannot tell 'design review' from 'broken profile'
- FIXED on both halves. The fact now travels: src/verdict_mcp/harness.py:1093 merges it into the state - `**({"no_gates": facts["no_gates"]} if facts.get("no_gates") else {})` - where run 4 found the merge dropping it.
- And the validator now refuses on it: src/verdict_mcp/validate.py:320-324 - `if state.get("no_gates"): return ["verdict is `pass`, but this run ran no gates at all ... Fix the profile ... or say `pass with risks` and name the gap in not_tested"]`, placed BEFORE the `if not isinstance(gates, dict) or not gates: return []` early exit that made the check unreachable.
- FIX-VERIFIED by counterfactual, old validate.py vs new over the identical state (verdict 'pass', gates {}, no_gates set): OLD returns no no-gates violation; NEW returns 'verdict is `pass`, but this run ran no gates at all - neither --gate nor a profile front-matter block supplied one, so nothing was measured. Fix the profile...'. Same input, opposite answers.
- The distinction run 4 asked for is preserved rather than flattened: an empty `gates` with no `no_gates` fact - a hand-built or pre-upgrade state - is still left alone, and tests/test_validate.py's new test_empty_gates_without_the_fact_is_left_alone pins that, while test_a_run_that_ran_no_gates_cannot_pass_unqualified pins the refusal and that `pass with risks` stays available.
- The judgment call is recorded in the code where the next reader will find it: validate.py:306-316 names VERDICT-F-17 and states the reasoning - the agent contract already said a missing gates block is 'report that and fix the profile', so the validator was aligned to the prompt rather than the other way round.

### VERDICT-F-26 — NEW — Major/P1 — REAL_DEFECT

Fix verification picks its test by scraping any pytest node id out of a finding's prose evidence, so a mis-scraped id that fails at HEAD force-reopens a correctly-resolved finding over the tester's judgment
- src/verdict_mcp/harness.py:273-274 - cited_tests() runs the regex _TEST_ID over every evidence string: `for line in finding.get("evidence") or []: ids += _TEST_ID.findall(str(line))`. Evidence is prose. Any node id quoted inside it - a code excerpt, a cross-reference to a different finding's test - is taken as this finding's demonstrating test.
- src/verdict_mcp/harness.py:398 - only the first is ever used: `test_id = tests[0]`. Selection is therefore evidence ORDER, not relevance.
- LIVE THIS RUN: facts.json verification.VERDICT-F-20 records test 't.py::new', at_previous 'error', at_head 'error'. There is no t.py in this repository. The id was scraped from F-20's own evidence line quoting a test's source: `assert facts["test_ids"]["added"] == ["t.py::new"]`. F-20 is genuinely fixed this run and could not be auto-verified because the harness ran a fabricated test.
- PROVEN by counterfactual in a scratch two-commit git repo (tempfile dir, never the checkout), using the shipped collect() and merge(): a finding whose evidence[0] cross-references an unrelated still-failing test (`test_other.py::test_unrelated_known_failure`) and whose evidence[2] cites its own real test (`test_pkg.py::test_pending`). cited_tests returned ['test_other.py::test_unrelated_known_failure', 'test_pkg.py::test_pending'] - the cross-reference first.
- PROVEN consequence, same probe: the defect WAS fixed (pkg.py q-i -> q+i) and the judgment said status 'resolved'. merge() returned status 'open', delta 'STILL_OPEN', resolution_refused 'test_other.py::test_unrelated_known_failure still fails at HEAD - measured by verdict-facts, so the resolution is not accepted'. A correct resolution was overturned by a measurement of an unrelated test.
- src/verdict_mcp/harness.py:443-452 - _apply_verification applies this before the outcome is derived, and it overrides both an explicit `resolved` and silence, so the wrong STILL_OPEN also reaches outcomes.json as an undecided finding and depresses the calibration track record the agent contract (§9) says the model must not be able to influence.
- The cross-check already exists and is not used: facts["_test_ids"] - the collected id set - is assigned at harness.py:871, two lines before verify_findings is called at harness.py:873, and is never passed in. An id absent from the collected set cannot be this suite's test.
- tests/test_verification.py:261 test_cited_tests_reads_ids_not_line_references pins exactly the permissive behaviour: it asserts that an id inside the prose 'see tests/test_c.py::TestK::test_m: assert 1 == 2' IS returned. The suite guards the scrape rather than the selection, so nothing goes red on either failure mode.
- Root cause: cited_tests() treats evidence prose as a citation index and _TEST_ID matches any `<path>.py::<name>` anywhere in it, including inside quoted source; verify_findings then takes tests[0], so the chosen test is whichever id appears earliest in the narrative; _apply_verification treats that test's HEAD result as authoritative over the tester's status → the feature landed whole in e71121a (v0.53.0, 'the loop closes - the harness verifies fixes') in this range; the permissiveness is deliberate at the extraction site (its test asserts it) and the risk was not carried through to the selection and override sites
- Class: "searched src/ for other consumers of cited_tests: verify_findings is the only one, and _apply_verification the only consumer of its record - so two sites, one pattern. The wider shape is 'a measureme

### VERDICT-F-29 — NEW — Major/P1

The diff-coverage step writes a 95 MB coverage.json plus coverage.db and coverage.rc into the committed team-mode QA root, gitignores none of them and deletes none of them, so the next `git add .qa` puts a ~95 MB blob into the repository permanently
- src/verdict_mcp/harness.py:562 - `rc, db, out = qa_root / "coverage.rc", qa_root / "coverage.db", qa_root / "coverage.json"`. All three land in the QA root, which in team mode IS the committed .qa/ directory.
- MEASURED THIS RUN: `ls -la .qa/coverage.*` -> coverage.json 94,987,311 bytes (95 MB), coverage.db 221,184 bytes, coverage.rc 279 bytes.
- `git check-ignore -v .qa/coverage.db .qa/coverage.json .qa/coverage.rc` prints nothing and exits 1 - none of the three is ignored. `git status --porcelain` lists all three as untracked (`?? .qa/coverage.db`, `?? .qa/coverage.json`, `?? .qa/coverage.rc`).
- .qa/.gitignore lists facts.json, judgment.json, state.json.prev and run-in-progress.json and states the policy in its own header: 'Per-run intermediates: the harness rewrites these every run ... What belongs in git is the record - state.json, the reports, the run history, the outcome ledger, the profile - not the scratch a single run passed through.' The three coverage files are exactly that scratch and were not added when diff coverage shipped in 6e6a859 (v0.54.0).
- They are never removed. harness.py:566-567 does `db.unlink(missing_ok=True); out.unlink(missing_ok=True)` at the START of the next run only, so between runs the 95 MB file simply sits in the QA root.
- profile.md:52-53 records that committing .qa/ is the maintainer's act and VERDICT-F-2 makes it necessary for the self-gate, so the commit that captures this is expected rather than hypothetical.
- GitHub refuses any file over 100 MB on push; 95 MB is 5% under the wall and coverage.json scales with tests x changed lines, which grew 9.2% in test count in this range alone.
- Second-order, same line: harness.py:591 reads the whole file with `json.loads(out.read_text(encoding="utf-8"))`, so the render step also holds ~95 MB of JSON plus its parsed object in memory in one go.
- Root cause: the QA root is used as scratch space for the coverage run (COVERAGE_FILE and the rendered JSON both point into it) while .qa/.gitignore enumerates intermediates by name, so a new intermediate is un-ignored by default; the `--show-contexts` JSON carries a per-line list of test contexts, which is what makes it two orders of magnitude larger than the database it came from → 6e6a859 (v0.54.0, 'which changed lines any test executed') added _diff_coverage and its three files in this range; the accompanying .gitignore was not extended - the commit added no .qa/.gitignore change (git diff --stat for the range lists .qa/.gitignore not at all)
- Class: "searched the harness for other writes into qa_root: state.json, state.json.prev, facts.json, judgment.json, test-ids.txt, runs.jsonl, outcomes.json, run-in-progress.json, reports/. Every one is eithe

### VERDICT-F-25 — NEW — Major/P2 — REAL_DEFECT

Fix verification copies the cited test back to the old commit only when the whole test FILE is absent there, so a regression test appended to an existing test file yields at_previous 'error' and fix_verified is silently never set
- src/verdict_mcp/harness.py:407-412 - the copy-back is file-level and conditional on absence: `test_file = test_id.split("::", 1)[0]; src, dst = Path(repo) / test_file, scratch / test_file; if not dst.exists() and src.is_file(): ... shutil.copyfile(src, dst)`. When the file exists at the previous commit, the scratch checkout keeps the OLD file - which does not contain the newly added test function.
- PROVEN by counterfactual in a scratch two-commit git repo (tempfile dir, never the checkout), using the shipped collect() and merge(): commit A has test_pkg.py containing only test_signature; commit B fixes pkg.py and APPENDS test_pending to the same file. Verification record: {"test": "test_pkg.py::test_pending", "at_previous": "error", "at_head": "pass", "summary": "(no match in any of [<Module test_pkg.py>]) -> 1 passed in 0.00s"}. merge() returned fix_verified: None on an explicit `resolved`.
- The control is the shipped test's own scenario: tests/test_verification.py:129 test_a_test_the_fix_added_is_carried_back_to_the_old_commit builds the same repo with `bugged_repo(tmp_path, with_test=False)` - no test file at all at commit A - and asserts test_copied_from_head is True and (at_previous, at_head) == ('fail', 'pass'). The only difference between the passing case and my failing one is whether the file already existed.
- Its own docstring claims the covered case is 'the commonest shape: fix and regression test land together'. That is right about the shape and wrong about the file: appending to tests/test_x.py is at least as common as creating it. This range is the proof - tests/test_code_drift.py gained 3 tests, tests/test_gate.py 2, tests/test_stop_hook.py 2, tests/test_harness.py 2, tests/test_validate.py 2, all appended to files that already existed.
- Fails silent, not loud: `error` is not `fail`, so _apply_verification (harness.py:443) takes neither the refusal branch nor the fix_verified branch. The finding resolves normally with no verification, and nothing in the report says the check was attempted and could not conclude.
- The consequence is the exact metric the feature was built for - harness.py's own module docstring cites '95 of 110 Sales findings undecided, no precision rate publishable'. fix_verified is what feeds outcomes.json, so under-verification keeps the track record unpublishable while looking like it is working.
- Root cause: the counterfactual is constructed at file granularity while the thing that must travel back to the old commit is a test FUNCTION; `if not dst.exists()` reads presence of the file as presence of the test → e71121a (v0.53.0) in this range, along with the whole verification feature; the guarding test was written for the with_test=False shape and the file-exists shape was never constructed
- Class: "searched the verification path for other file-vs-symbol granularity assumptions: _scratch_checkout copies whole trees and _one_test_cmd rewrites only the interpreter, so this is the only site. It sha

### VERDICT-F-27 — NEW — Major/P2

verdict-issues dedupes on finding id alone, so a REGRESSED finding - the class the contract ranks first - is never re-filed and the run reports it as 'already filed'
- src/verdict_mcp/issues.py:94 - the whole dedupe: `out.append((f, "exists" if fid in ledger else "create"))`. Membership of the id in the ledger is the only test.
- src/verdict_mcp/issues.py:195 - the ledger entry DOES store the discriminator and nothing ever reads it back: `ledger[fid] = {**made, "hash": f.get("hash"), "created_at": ..., "run_number": ...}`. grep for `["hash"]` / `.get("hash")` in issues.py returns the write site only.
- PROVEN by probe using the repository's own gh stub (tests/test_issues.py helpers, VERDICT_HOME pointed at a tempdir): run 1 with one open Critical finding filed issue #1 and wrote the ledger entry {"W-F-1": {"hash": "w-f-1", "number": 1, ...}}. The state was then changed to the regression shape - same id, hash 'brandnewhash', delta 'REGRESSED', new title and new evidence - and run 2 with --create produced: 'verdict-issues: created 0, 1 already filed, 0 deferred by --limit', with a total of 1 gh invocation across both runs.
- The report is not just silent but wrong: the operator is told '1 already filed', which asserts the tracker is current for that finding. In the regression case the earlier issue was in all likelihood closed when the finding resolved, so the tracker holds a CLOSED issue and no open one.
- agents/verdict.md §6 ranks this class above every other delta: 'REGRESSED - was resolved, is back <- rank these first, always'. The one integration that carries findings to the people who fix them is the one that drops it.
- tests/test_issues.py:116 test_a_second_run_files_nothing_twice pins idempotence for the unchanged case and no test constructs a resolved-then-regressed finding, so the suite stays green over this.
- Same shape, lesser cost: the ledger is also never reconciled with the tracker, so an issue a human deletes or transfers is never re-filed either. Not probed - the stub cannot express it.
- Root cause: the ledger key is identity (the id, which by contract is minted once and never reused) while the question being asked is recurrence (has THIS occurrence been filed) - the stored `hash`, which is the recurrence fingerprint, is written and never compared → 32d1c41 (v0.56.0, 'findings meet the tracker - verdict-issues'), the last commit in this range; the module is new in this range and this is its first QA pass
- Class: "searched issues.py for other identity-vs-occurrence conflations: plan() is the only decision point and load_ledger/save_ledger are pure IO, so one site. The same conflation would appear in any future

### VERDICT-F-28 — NEW — Major/P2

Diff coverage is measured in-process only, so code exercised by subprocess-invoking tests is reported as 'not imported by anything the suite executed' with every changed line unexercised - a false measured claim, and a cry-wolf risk for the new pass-refusal rule
- src/verdict_mcp/harness.py:563-564 writes the coverage config the run uses: `[run] dynamic_context = test_function, relative_files = True, data_file = ...` and `[json] show_contexts = True`. There is no `concurrency`, no `parallel`, and no COVERAGE_PROCESS_START - grep for all three across src/ returns nothing. coverage.py therefore records only the pytest process.
- LIVE FALSE CLAIM THIS RUN: facts.json coverage.per_file['src/verdict_mcp/issues.py'] = {"changed": 217, "measured": 217, "executed": 0, "note": "not imported by anything the suite executed"} - while tests/test_issues.py contains 9 tests that execute exactly that file, via tests/test_issues.py:74 `subprocess.run([sys.executable, str(ISSUES), "widget", "--gh-cmd", ...])`. All 9 passed in this run's suite gate.
- Same for the hooks: hooks/enforce_run_contract.py (22 changed lines, executed 0) and hooks/report_open_findings.py (5 changed lines, executed 0) carry the same note, and tests/test_stop_hook.py:26 runs them the same way - `subprocess.run([sys.executable, str(HOOK)], input=...)`.
- 244 of this run's 1125 changed lines - 22% - are misreported as unexercised on that basis, which is why the headline reads 63% rather than a number that reflects what the suite actually ran.
- The renderer states it as fact in the report: harness.py:1634-1640 emits `  - <path>: unexercised lines <ranges> (<note>)` for every per-file entry with unexercised_ranges, so 'src/verdict_mcp/issues.py: unexercised lines ... (not imported by anything the suite executed)' is written into the shipped artifact.
- The exactness claim is explicit and is what makes this a defect rather than a known limit - harness.py:476-478: 'Per-test attribution is a lower bound (a tracer may record a line under one context and skip it under the next); "executed by any test" is exact, and that is what the gate is built on.' It is not exact for subprocess-executed code.
- LATENT: src/verdict_mcp/validate.py:267-281 (_unexercised_diff, new in this range) refuses an unqualified `pass` when changed_lines > 0 and changed_lines_executed == 0. A range confined to CLI or hook code - entirely plausible in this repo, which tests both by subprocess - measures a true zero and gets its correct `pass` refused. It did not fire this run only because the range also touched in-process code (705/1125).
- This project's own profile names the shape: the plugin is a CLI plus hooks, so subprocess testing is the idiom here rather than an exception.
- Root cause: coverage.py traces only the process it starts unless subprocess support is configured; the harness generates its own rc file and omits it, and the fallback branch at harness.py:602-608 then treats 'coverage never saw this file' as 'no test executed these lines', which are different statements → 6e6a859 (v0.54.0) in this range introduced _diff_coverage and the generated rc together
- Class: "swept the repo for the affected shape rather than the affected file: `grep -n 'subprocess.run(\\[sys.executable' tests/` names test_issues.py, test_stop_hook.py and the gate/hook tests - so every CLI

### VERDICT-F-13 — STILL_OPEN — Minor/P2 — age 3d

fixture_freshness reproduces the fixture lossily: content of tracked files only, so mode changes, symlink swaps and planted untracked files are invisible, and a missing tracked file tracebacks
- STILL OPEN at HEAD 32d1c41, both cited lines byte-identical. eval/fixture_freshness.py:44 is still `listed = _git(["ls-files", "-z", *PAIR], REPO).split("\0")` - tracked files only, so a planted untracked file is outside the comparison by construction.
- eval/fixture_freshness.py:54 is still `shutil.copyfile(REPO / rel, dest)` - content, not mode; the single line behind three of the five gaps run 3 probed (chmod +x invisible, symlink swap invisible, FileNotFoundError traceback on a deleted tracked file).
- Untouched in this range: `git log --oneline c386317..HEAD -- eval/fixture_freshness.py` is empty. The 34-file diff does not include it.
- The gate itself passed this run in 0.06s: 'fixture_freshness: OK - pricer-delta.diff still describes the fixture pair (78 lines)', exit 0. The main job - detecting a content edit to the tamper-evidence anchor - is intact; this finding is about the edges it cannot see.
- HONEST LIMIT, unchanged from run 4: the five probes were not re-executed this run. The finding is carried on unchanged source at the two cited lines, which establishes the mechanism persists and is weaker than a fresh reproduction.

### VERDICT-F-15 — RESOLVED — Minor/P3 — BRITTLE_TEST — age 3d

The stale-command-name guard sweeps README-pypi.md, docs/ and commands/ but still does not reach standards/ or templates/, so a dead command name can ship in those trees unnoticed
- FIXED at tests/test_commands.py:66-73 - the sweep now includes `*REPO.glob("standards/*.md"), *REPO.glob("templates/*.md")`, and two new assertions pin the reach itself rather than only its result: `assert any(p.name == "release-gate.md" for p in live)` and `assert any(p.name == "test-case.md" for p in live)`.
- FIX-VERIFIED by re-injection in a scratch copy of the doc trees (tempfile dir, never the checkout): appending 'Run `/verdict:qa-delta` nightly.' to standards/release-gate.md makes the shipped test fail - 'AssertionError: standards/release-gate.md: stale command name /verdict:qa-delta', exit 1.
- And the counterfactual closes it: run 4's version of the same test, `git show c386317:tests/test_commands.py`, run against the SAME planted tree, passes - '1 passed in 0.01s', exit 0. The guard now catches what it demonstrably did not catch before.
- The narrower-than-it-looks scope is preserved: the regex still names only the eleven commands that have ever existed, so the docs/project-key.md 'qa-nightly' directory name that made an earlier widening cry wolf is still not matched.
- Age at resolution: first seen 2026-08-30, so this finding was open 3 days and survived one partial fix (the widening to README-pypi.md and docs/) before the remainder landed - the partial fix is what lessons.md records as having nearly earned it a second id.

### VERDICT-F-22 — STILL_OPEN — Minor/P2 — age 1d

The VERDICT_STRICT bash guard reads '>' and sed expressions inside quoted strings as output redirection and resolves the phantom target against the checkout, blocking read-only QA commands
- RE-PROVEN three more times this session, on three read-only commands, with the checkout unmodified throughout. (1) `python3 -c` whose source contained `if len(s)>1500:` - refused: "output redirection targets '<checkout>/1500:'". (2) A python -c printing the label `'derive_key ->'` - refused: "output redirection targets \"<checkout>/',\"". (3) A heredoc-fed python script containing the string "def index_row(state: dict) -> str:" - refused: "output redirection targets '<checkout>/str:\")[1].split(...)'".
- Case (3) is new information and sharpens the mechanism: the blocked token was inside a `<<'PY'` heredoc body, where by construction no shell redirection can occur. The scan is applied to the raw command text before the shell's own parse, so quoting AND heredoc framing are both invisible to it.
- '->' is the trigger in two of the three, and it is a Python return annotation - it appears on 100+ lines of this repository's own source, which is the code the guard is meant to let a tester read.
- hooks/enforce_bash_scope.py is untouched in this range: `git log --oneline c386317..HEAD -- hooks/enforce_bash_scope.py` is empty, so the mechanism is unchanged since it was filed.
- Still Minor and still failing in the safe direction: no unsafe command was permitted this session, and every refusal was recoverable by rephrasing. Cost this run: three commands and two rewritten probe scripts.
- Scope bound unchanged: I did not read the guard's full token list this run either, so the origin link of the root cause stays a hypothesis.

### VERDICT-F-18 — RESOLVED — Minor/P2 — age 1d

code_drift conflates 'shallow clone' with 'full clone, recorded commit absent' as `unknown`, and every renderer is silent on `unknown` - so a state anchored to a vanished commit reports nothing at all
- FIXED at src/verdict_mcp/state.py:696-710 - a new `absent` status, raised only when the recorded sha is commit-shaped AND `git rev-parse --is-shallow-repository` says false, so a garbled sha stays `unknown` rather than crying wolf.
- FIX-VERIFIED by counterfactual on this repository, same input to both versions: `code_drift(repo, 'deadbeef...deadbeef')` returns {'status': 'absent'} at HEAD and {'status': 'unknown'} under c386317's state.py loaded side by side in one process.
- The silence is closed at all three renderers, which was the half that made the finding matter: gate.py:127-131 exits 5 on `absent` under max_commits_behind, gate.py:205-207 renders 'measured on a commit this repository does not contain', and hooks/report_open_findings.py:105-109 says the same in the SessionStart banner.
- Guarded by three new tests, all passing in this run's suite: tests/test_code_drift.py::test_a_complete_clone_missing_the_commit_is_absent_not_unknown, ::test_a_shallow_clone_that_cannot_see_the_commit_stays_unknown, ::test_banner_names_a_commit_the_repository_does_not_contain.
- The cry-wolf boundary the original finding worried about is explicitly held: state.py's own comment records it - 'A garbled recorded sha is corrupt input, not an observation about the repository, and an alarm over garbage is the false "you are behind" that gets the line ignored.'

### VERDICT-F-24 — RESOLVED — Minor/P2 — age 1d

The INDEX row is composed from unmeasured values: its Date cell comes from the local-clock date.today() rather than the run's measured UTC timestamp, and its Delta-tests cell is hardcoded n/a even when the set-diff was measured
- FIXED at src/verdict_mcp/harness.py:1149-1156 - the Date cell is now sliced from the run's own measured stamp (`stamp = str((state.get("last_run") or {}).get("timestamp_utc") or ""); day = stamp[:10]`) and the delta cell is rendered from the measured set-diff counts instead of the literal 'n/a'.
- FIX-VERIFIED by counterfactual on the delta half, old index_row vs new over an identical state carrying test_ids {status measured, added_count 51, removed_count 1}: OLD emits '... | 593 / n/a / n/a | n/a | ...', NEW emits '... | 593 / n/a / n/a | +51/-1 | ...'.
- The date half could not be flipped by counterfactual TODAY and I am saying so rather than claiming it: this host is UTC-7 and the run began at 15:38Z / 08:38 local, so date.today() and the stamp's date agree and both versions print 2026-09-02. The defect is a west-of-UTC-after-17:00 condition. What is verified is that the new code reads the stamp and no longer calls date.today() for this cell.
- Backstop added in the same range, which is what makes the residual acceptable: harness.py:1284-1291 check_artifacts re-reads the INDEX after finalize and reports 'INDEX row is dated X, the state was measured Y' - so the date half is now caught by measurement even if the renderer regresses, and tests/test_artifact_check.py::test_an_index_row_dated_from_the_wrong_clock_is_named guards it.
- Residual for the maintainer, not a new finding: the existing 2026-09-01 INDEX row for run 4 is still wrong on disk. finalize does not rewrite history.

### VERDICT-F-30 — NEW — Minor/P2

The report's fix-verification line counts the agent's own fix_verified claim inside a block sourced from harness measurements, so a finding the harness measured as error/error is published as '1 verified'
- src/verdict_mcp/harness.py:1616-1623 - the block is selected by measurement (`measured = [f for f in findings if isinstance(f.get("verification"), dict)]`) and then counted by claim (`verified = sum(1 for f in measured if f.get("fix_verified") is True and f.get("delta") == "RESOLVED")`). `fix_verified` is the agent's judgment field; `verification` is the harness's measurement. One line mixes them.
- LIVE IN THIS RUN'S OWN REPORT: .qa/reports/2026-09-02-delta.md renders 'Fix verification: 1 verified - 0 refused (cited test still fails at HEAD) - 0 measured but not verifiable'. The harness produced exactly one verification record this run, for VERDICT-F-20, and it reads at_previous 'error', at_head 'error', summary 'ERROR: file or directory not found: t.py::new'. The harness verified nothing. The '1' is my own fix_verified flag on F-20.
- The arithmetic then empties the bucket that would have shown it: `len(measured) - verified - refused` = 1 - 1 - 0 = 0, so 'measured but not verifiable' reads zero on a run whose only measurement was unusable. The error case is not merely uncounted, it is displaced into the success count.
- Independent of VERDICT-F-26, which supplied the bad id here: even with a correctly chosen test, a record of pass-at-both-commits (which harness.py:358-361 documents as verifying nothing) plus an agent-set fix_verified would render identically as 'verified'.
- agents/verdict.md §9 draws the line this display erases: 'You never set the outcome ... Marking your own work correct is the one thing a tester must never be able to do', with fix_verified as the single field the agent may set - and it costs a demonstration the agent cites, not one the harness ran.
- Root cause: the set is chosen by the presence of a measurement and the count is taken from a judgment field on the same finding; nothing in the count consults the measurement's own at_previous/at_head result → e71121a (v0.53.0) in this range, which added both the measurement and this renderer
- Class: "searched the renderer for other counts that cross the measurement/judgment boundary: the gate table, the test-id ledger and the diff-coverage block all read facts-derived fields only. This is the sin


## Track record

29 findings tracked across this project's history · 14 settled, 15 still undecided.

| Confidence claimed | Held up | Withdrawn | Rate |
|---|---|---|---|
| proven | 10 | 0 | _not yet_ |
| unstated | 3 | 0 | _not yet_ |
| probable | 1 | 0 | _not yet_ |

*A rate appears once a row has 30 settled outcomes. Settled means fix-verified or regressed (it held up) against withdrawn (it did not); a finding merely resolved is not evidence either way.*

## Release blockers

_None._

## Verified intact

- The suite is wholly green and grew: `uv run --group dev python -m pytest tests/` exits 0, 593 passed in 57.95s, no skips, no xfails, no collection errors. Run 4 measured 543 passed.
- No silent test drop. Id set-diff: +51 / -1 against the stored ledger, and 543 + 51 - 1 = 593 reconciles exactly. The single removed id, tests/test_validate.py::test_a_run_with_no_gates_claims_no_suite_and_is_left_alone, was deliberately inverted by the VERDICT-F-17 fix and replaced by two tests (test_a_run_that_ran_no_gates_cannot_pass_unqualified, test_empty_gates_without_the_fact_is_left_alone) that cover both the new rule and the old carve-out.
- The fixture tamper-evidence anchor holds: `python3 eval/fixture_freshness.py` exits 0 in 0.06s - 'pricer-delta.diff still describes the fixture pair (78 lines)'. The answer keys, corpus and delta fixture were neither regenerated nor touched this run.
- The run-history chain is intact and now covers this repository: verify_chain over .qa/runs.jsonl returns status 'intact', and harness_signals reports state_computed, report_rendered and chain_intact all true, missing_durable == []. Run 4's recorded live exposure (rows 2 and 3 unchained) is closed for the rows that exist.
- The scope guards both held, in the direction that matters. The Bash guard refused three of my read-only commands and permitted no write into the checkout; `git status --porcelain` at the end of the session shows no modification to any tracked file under src/, tests/, eval/, hooks/, standards/ or templates/ - only .qa/ artifacts and the untracked coverage files.
- Suite duration is within the gate: 57.95s against run 4's 52.07s is +11.3% on the suite gate - marginally over the 10% week-over-week guide, but the suite also grew 9.2% in test count over the same range (543 -> 593), so per-test cost fell. No duration_regressed flag was raised by the harness against its own five-run window.
- The three eval fixtures with intentionally seeded defects are still excluded from the project suite (pyproject.toml:90), so no seeded defect is being counted as a green test.

## Not tested

- Windows and the py3.10 leg of the CI matrix - not testable on this machine; the range touched harness.py, issues.py and state.py, all of which do path and subprocess work (`shlex.quote` vs the nt branch in `_one_test_cmd`, `gh.cmd` shimming in issues.py).
- verdict-issues against a real `gh` and a real repository. Every issues.py probe this run used the shipped stub. Nothing was filed anywhere, by design (dry-run wall) and by constraint.
- The GitHub Action (action.yml) in either mode. `run` mode is forbidden locally by the profile.
- The eval corpus and scorer end-to-end - the agent-prompt regressions the profile ranks as risk area 2 are only visible there, and the corpus was not run this session.
- Mutation testing / suite quality: no mutation tool is present in this repo, so the strength of the 593 green assertions is unmeasured. Two ad-hoc mutations were done by hand instead (VERDICT-F-15 re-injection, and the F-25/F-26 counterfactual repositories).
- hooks/enforce_bash_scope.py's full token list - VERDICT-F-22's root cause is still only a hypothesis about mechanism because the tokenizer was not read line by line.
- The remaining ~420 unexercised changed lines outside the four files I read closely (harness.py verification and diff-coverage paths, issues.py, validate.py, gate.py): the docs/ and CHANGELOG.md prose in the range was not proof-read against the code it describes.
- Concurrency: two verdict-facts runs against one QA root. The run marker and the run_number re-read exist for it; neither was exercised.

## Fix order

Ordered for dependencies, not severity alone. Items 1 and 2 are independent of everything else and cost minutes; 3-5 are in one file and share a test harness, so they want one sitting.

1. **VERDICT-F-29** - add `coverage.rc`, `coverage.db`, `coverage.json` to .qa/.gitignore. Do this before the next `git add .qa`, because after it the fix is a history rewrite rather than a line. Separately, have _diff_coverage remove coverage.json when it is done reading it, and consider streaming or `coverage json` without --show-contexts when the file exceeds a size bound.
2. **VERDICT-F-26** - pass the collected id set (facts['_test_ids'], already computed two lines earlier at harness.py:871) into verify_findings and drop any cited id not in it. Then prefer an explicit `verification_test` over anything scraped, and when more than one scraped id survives, verify none and note it rather than guessing at tests[0]. Do this before F-25: F-25's fix changes what runs at the old commit, and there is no point making the wrong test run correctly.
3. **VERDICT-F-25** - after F-26 has narrowed the id to a real one, make the copy-back symbol-aware: copy the test file from HEAD whenever the named test does not resolve at the old commit, not only when the file is missing. `--collect-only <id>` in the scratch tree answers 'does this test exist here' cheaply. Guard it with the probe this run used - existing file, appended test - because the current suite passes over the bug.
4. **VERDICT-F-27** - compare the ledger entry's stored `hash` against the finding's current hash in plan(), and treat a changed hash as `create`. The field is already being written; only the read is missing. Decide separately, and with the maintainer, what a re-filed regression should say about the earlier issue number.
5. **VERDICT-F-28** - enable subprocess coverage (`concurrency = multiprocessing` / COVERAGE_PROCESS_START plus `coverage combine`) in the generated rc; failing that, change the note and the report line so they state what was actually measured ('not traced in-process; this project runs CLI and hook code as subprocesses') rather than 'not imported by anything the suite executed'. Until one or the other lands, _unexercised_diff's pass-refusal should be held to ranges that contain in-process code.
6. **VERDICT-F-21** - a maintainer decision as much as a fix: either anchor the chain outside the deletable file, or accept the risk and say so in the profile. Do not leave it as a third state.
7. **VERDICT-F-22** and **VERDICT-F-13** - both are bounded, both fail safe, and neither blocks. F-22 wants a quoting-aware scan (or at minimum an exemption for text inside a heredoc body); F-13 wants `copy2` and an `ls-files --others` sweep.
8. **VERDICT-F-30** - count the harness's own result in that line (at_previous 'fail' and at_head 'pass'), and show the agent-claimed fix_verified separately if it is worth showing at all. Cheap, and it should land with F-26 so the 'measured but not verifiable' bucket starts telling the truth about mis-chosen ids.

## Next run focus

- VERDICT-F-26 first, and check it against the state this run just wrote: re-read every finding's `verification` block and confirm the `test` field names a test that actually exists in test-ids.txt. Any record whose `test` is not a collected id was scraped out of prose, and any `resolution_refused` resting on such a record is a wrong STILL_OPEN in the system of record.
- VERDICT-F-29 second and cheaply: `git status --porcelain .qa/` and `du -h .qa/coverage.json`. If coverage.json is tracked in git by then, the repository has permanently gained a ~95 MB blob and the remedy is no longer a .gitignore line.
- VERDICT-F-25: after any fix, re-run the two-commit probe where the regression test is APPENDED to an existing test file (not a new file) and confirm at_previous reads `fail`, not `error`. The shipped test only covers the new-file case, so the suite will stay green through a wrong fix.
- VERDICT-F-21 is unchanged and now the oldest open Major: the delete-the-ledger evasion still returns chain_intact true. Re-run the probe (copy .qa to a scratch dir, unlink runs.jsonl, drop last_run.chain, call harness_signals) before deciding whether to accept the risk instead.
- Read the ~420 changed lines this run did not: the docs/state-schema.md and CHANGELOG.md additions against the code they describe, and hooks/enforce_run_contract.py's new missing_durable branch (22 changed lines, 0 executed in-process - it is a subprocess hook, so VERDICT-F-28 applies to its coverage number too).

## Notes

**Risk ranking and where the budget went (§8.2).** The surface was ranked by change volume x blast radius x this project's own defect history, computed from the state file rather than recalled: merging suffix-equivalent paths, src/verdict_mcp/harness.py carries severity weight 16 across 6 open findings and src/verdict_mcp/state.py weight 14 across 4 - by a distance the two hottest files in four recorded runs, and harness.py is also the largest change in this range (588 changed lines). That is where the reading went, and three of the five new findings are there. Second tier: src/verdict_mcp/issues.py (217 new lines, entirely new module, the only code in the repository that mutates a third-party account) - read in full, one finding. Third: validate.py and gate.py (79 lines between them) - read, no findings. **The cutoff fell below the ~420 remaining changed lines in docs/, CHANGELOG.md and the test files themselves**, and it fell there for time, not tooling: verifying six inherited fixes by counterfactual consumed roughly half the run. Everything below the line is in not_tested.

Hold the clustering honestly: four recorded runs is a snapshot, not a trend, and harness.py's weight partly reflects that harness.py is simply where the project has been working.

**Assertion sweep of the green suite (§3).** The 593 passing tests were not taken at face value on the changed files. tests/test_verification.py, tests/test_issues.py, tests/test_diff_coverage.py, tests/test_artifact_check.py and tests/test_code_drift.py were read for assertion quality. They are good tests - real two-commit git repositories, a recording `gh` stub, real coverage databases, no mock asserting its own return value. Two gaps rather than defects, both reported inside the findings they belong to: test_verification.py:129 pins the copy-back only for the new-file case (VERDICT-F-25) and test_verification.py:261 pins the permissive id scrape as intended behaviour (VERDICT-F-26). No BRITTLE_TEST finding is filed this run.

**AI-authorship census (§4.5).** code_census reports 0 of 9 commits AI-attributed, with the harness's own caveat that absence of trailers is not evidence of human authorship. Placeholder leads: 2 TODO, 8 'for now', 1 swallowed except - swept, and all but one are inside .qa/ reports and CHANGELOG prose quoting earlier findings. Undeclared-import leads (`conftest`, `state`) are the repository's own sys.path-insert idiom for bare-script hook execution, not hallucinated dependencies. Nothing filed from the census; it was used as a reading map only.

**Deviations and blocks.** The bash scope guard refused three read-only commands this session; each is recorded as fresh evidence under VERDICT-F-22 and each was recovered by rephrasing. No command was routed around the guard. Nothing under src/, tests/, eval/, hooks/, standards/ or templates/ was modified: every probe ran in a tempfile scratch directory, and the two that needed old code loaded it via `git show` into a temp module rather than checking anything out.

**One thing this run could not settle.** VERDICT-F-24's date half is fixed in code but could not be flipped by counterfactual today, because this host's local date and the run's UTC date happen to agree. It is marked fix_verified on the strength of the delta-cell counterfactual and the new check_artifacts backstop, and the limit is stated on the finding rather than buried here.

**Found by reading the artifact back, not the source.** VERDICT-F-30 was invisible until this run's own rendered report was read after finalize - the same discipline the 2026-09-02 lesson records, and the second consecutive run where reading the written artifacts produced a finding that reading source did not.

**Correction, recorded rather than hidden.** This run was finalized twice. The first finalize produced the report in which VERDICT-F-30 became visible; the finding was then added and the run re-finalized through the harness's own correction path, so runs.jsonl carries run 5 twice (revision 0 and revision 1) and the chain extends over the correction rather than around it. Same facts.json, same measurements, one added finding. Run 4 was corrected the same way.

---

*Countable sections rendered from `state.json` by `verdict-finalize`; the prose is the agent's. They cannot disagree.*
