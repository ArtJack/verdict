# QA report — verdict · run 10 (delta)

**VERDICT: pass with risks**

## Scope

- Range: `a6011ec465aaf1fe793c2e7f4f002fcb999770e9..d21d4f93c5868fea479e903710ada4d1ccf3df40` · 22 files changed, 1523 insertions(+), 460 deletions(-)
- Branch: `main` · measured 2026-09-03T01:44:59Z
- Isolation check: **pass**

Delta run 10 over `a6011ec..d21d4f9` — three releases (v0.71.0, v0.72.0, v0.73.0) plus commit 3bbce81, which touches `.qa/` only and was read as the baseline record rather than audited as code. 22 files, 1523 insertions, 460 deletions; excluding `.qa/`, 14 files and 447 insertions. Two days since the last run and well inside the re-baseline thresholds, so this is a delta run against run 9's state, not a fresh audit. Mode is team: the in-repo `.qa/` is the only system of record consulted or written; no user-level QA root, no `$VERDICT_HOME`, no network path was read or written, and nothing in this session asked me to.

**The stale marker.** `.qa/run-in-progress.json` existed at start, stamped `2026-09-02T23:44:23Z` against HEAD's own sha, two hours before this run began. It is gitignored per-run scratch (`.qa/.gitignore:5`), so it cannot be a teammate's committed state; it is the residue of a crashed or superseded attempt at this same commit. Nothing was finalized by it — `runs.jsonl` ends at run 9 and `state.json` still reads `run_number: 9` — so the run-history chain has no gap and this run continues it as run 10 rather than re-baselining. `verdict-facts` recorded it as `previous_attempt_this_run` with `age_hours: 2.01`, which is the right classification reached through a wrong sentence; see VERDICT-F-53.

**Risk budget.** The surface is small enough to name completely, so the ranking is short. Effort went, in order, to: the prompt change (0.73.0), because it alters the behaviour contract and the unit suite cannot see it; the scope-guard hooks, the profile's number-one risk area and the site of the range's other code change; the seven fixes claimed closed, each of which was checked by a route that would fail if the fix were not real; and the four findings carried in from earlier runs. Nothing was dropped for time. What was *not* attempted is in `not_tested` and is dominated by two items: no model run against the eval corpus, and no fresh full mutation campaign.

## Gates

| Gate | Result | Exit | Duration | Summary |
|---|---|---|---|---|
| `suite` | pass | 0 | 74.82s | 690 passed in 74.55s (0:01:14) |
| `fixture_freshness` | pass | 0 | 0.07s | fixture_freshness: OK — pricer-delta.diff still describes the fixture pair (78 lines) |

Tests: passed 690, collected 690
Test-id ledger: 690 ids · +16 / −0 (set-diff, not summary arithmetic)
Fix verification: 0 verified · 0 refused (cited test still fails at HEAD) · 1 measured but not verifiable
  - 9 open finding(s) cite no test id, so the harness cannot fix-verify them
Diff coverage: 133/153 changed lines executed (87%) across 8 file(s); 47 test(s) touch the diff
  - production 32/34 (94%) · test code 101/119 (85%) — read production first; a test file's fixtures and `def` lines never carry a test context, so the blended number moves with the shape of the diff
  - 16 of them only in a subprocess the suite spawned — measured, but no test to name
  - hooks/enforce_bash_scope.py: unexercised lines 330-331
  - tests/test_calibration.py: unexercised lines 514, 531, 559
  - tests/test_diff_coverage.py: unexercised lines 362, 398, 409
  - tests/test_fixture_freshness.py: unexercised lines 89, 92
  - tests/test_hooks.py: unexercised lines 589, 603, 614
  - tests/test_validate.py: unexercised lines 772, 785, 795, 802, 806, 815, 824

## Risks

The verdict is `pass with risks` and the risks are concentrated in two places.

**The safety guarantee is still not whole.** The product's headline promise — Verdict cannot modify your code — rests on the two scope hooks, and this run allows a checkout directory to be archived out and deleted with one reordering of a `tar` command line (VERDICT-F-42). The reported form is genuinely fixed; the shape is not. That the fix's own tests all use the token order the finding's title happened to quote is the interesting part, and the second bug in the same nineteen lines (VERDICT-F-49, opposite direction) says the handler needs an option table rather than another patch.

**The verification machinery can still lie, in a new way.** 0.73.0 closed the `.pth` hole and its recipe is correct as far as it goes. It does not go as far as the bytecode cache, and its stated check — print the module's `__file__` — passes cleanly while the wrong code runs (VERDICT-F-50). This run met that failure live: the first pass at re-measuring VERDICT-F-48 read 4 of 5 and the missing kill was an instrument artefact, not a gap in the suite. A tester that had stopped there would have filed a defect against a guard that works, and the same mechanism in the other direction turns a false green counterfactual into `fix_verified: true`, a `confirmed` ledger row and a published "held up". That is precisely the chain §9 exists to protect.

Beneath those, two smaller risks. The 0.73.0 release's central claim of measurement is unpublished and, on the fixture it names, unmeasurable (VERDICT-F-52) — the project's own two precedents published a dated row with a control, and this one did neither. And the Track record table, fixed twice now for saying more than it knows, still prints a number a reader can subtract their way past (VERDICT-F-51).

What is genuinely better: six of run 9's seven findings are closed, five of them fix-verified by isolated re-injection rather than by absence; the new production/test coverage split is honest under audit and pointed at a real defect on its first run; and the five mutants 0.72.0 wrote guards for all die when re-applied.

A third, smaller risk surfaced in this run's own output rather than in the range: the harness stamps finding dates from the host's local calendar while timestamping the run in UTC (VERDICT-F-54). It has been latent for nine runs because all of them were made in local daytime.

## Findings — REGRESSED first (10 open of 16 tracked)

### VERDICT-F-21 — STILL_OPEN — Major/P2 — age 1d

The run-history chain's anti-fabrication signal can still be shed by deleting data, and the gate then exits 0: 0.66.0 moved the ratchet into outcomes.json, but the anchor is itself unauthenticated, so removing one JSON key restores `unchained` with the whole track record intact
- Untouched by this range: `git diff a6011ec..HEAD -- src/verdict_mcp/state.py` changes only `_verification_digest`'s key tuple (the F-46 fix); tests/test_chain.py is not in the range at all.
- CHANGELOG.md:32-34 still states a price the deletion does not pay — unchanged this run.
- Awaiting a maintainer decision rather than a measurement: either the residual is accepted and the changelog corrected, or the ratchet moves out of the QA root. tests/test_chain.py:162-179 is the specification either way.

Unchanged and still waiting on a person. Two runs have now measured everything measurable about it; what is left is whether deleting a key from the QA root's own ledger is an accepted residual risk. If it is, CHANGELOG.md:32-34 states a price the deletion does not pay and should be corrected; if it is not, the ratchet has to live somewhere the run being audited cannot write.

### VERDICT-F-50 — NEW — Major/P1 — REAL_DEFECT

The isolation step 0.73.0 just added to the contract is defeated by a stale __pycache__, and the check it prescribes cannot see that: two same-size mutations of one file inside one second, and the second runs the first's bytecode
- Measured this run, same procedure twice, one variable changed. With `__pycache__` left in place: mutant M3 (validate.py:429 `continue` becomes `break`) reported `1 passed in 0.01s` for tests/test_validate.py::test_the_state_validator_keeps_checking_after_a_malformed_entry, i.e. SURVIVED. With `__pycache__` swept and PYTHONDONTWRITEBYTECODE=1: `1 failed in 0.05s`, i.e. KILLED. Killed 4 of 5 in the first pass, 5 of 5 in the second.
- Mechanism, printed: after applying M2 (validate.py:198 `continue` becomes `break`) the source is 31075 bytes at mtime second 1788400567 and a .pyc is written; applying M3 gives an identical 31075 bytes at the identical mtime second 1788400567. CPython validates a cached .pyc on (source mtime seconds, source size), so M3's source loads M2's bytecode.
- The prescribed check does not discriminate: in both runs `verdict_mcp.validate.__file__` printed the scratch path `.../scratchpad/tree/src/verdict_mcp/validate.py`. The path is right and the bytecode is wrong. `__cached__` was `.../tree/src/verdict_mcp/__pycache__/validate.cpython-313.pyc` in both.
- Behavioural read-out, same probe: with the stale cache `validate()` returned ['findings[0] is not an object', "findings[1] (PRC-F-2) severity 'Huge' not in [...]"] and the guard test PASSES (mutant reported SURVIVED); after deleting `__pycache__` it returned only ['findings[0] is not an object'] and the guard test FAILS (mutant KILLED).
- agents/verdict.md:221-227 (the paragraph 0.73.0 added) names PYTHONPATH and `print(<pkg>.__file__)` and says nothing about bytecode; docs/state-schema.md:319-326 repeats the same recipe for `verification.pythonpath`.
- `cp -a <checkout> <scratch>` — the copy form the bash guard was widened in 0.65.0 to permit — carries every `__pycache__` in the tree, so the trap is armed by the contract's own recommended procedure.
- Root cause: CPython's cached-bytecode validation compares the source's mtime (truncated to whole seconds) and its size against the header of the .pyc. Successive injections into one file during a fast re-injection loop routinely land in the same second, and two mutations of the same kind — here `continue` to `break` at two different lines — produce files of identical size. When both hold, the interpreter reuses the previous injection's bytecode and the new injection is never executed. → Introduced as a contract gap by d21d4f9 (v0.73.0), which added the isolation paragraph and specified the import-path check as sufficient. The underlying interpreter behaviour is not a defect in this repository; the defect is that the procedure this repository mandates does not control for it and its stated verification cannot detect it.
- Class: {"pattern": "searched for the same shape in the machine-side verifier: `_verify_findings`/`_run_test` in src/verdict_mcp/harness.py:399-534 sets PYTHONPATH but never sets PYTHONDONTWRITEBYTECODE and n

This is the run's sharpest finding and it lands on yesterday's fix. 0.73.0 was written because a counterfactual can silently measure the wrong tree; it named the `.pth` mechanism, gave the PYTHONPATH remedy, and offered `print(<pkg>.__file__)` as the check that the remedy took. The check is not sufficient, and the way it fails is instructive: the path is correct and the *bytecode* is stale, so the prescribed verification prints exactly what a healthy run prints. I hit it inside twenty minutes of following the new contract to the letter, on the very measurement that re-verifies VERDICT-F-48.

The conditions are not exotic. CPython validates a cached `.pyc` against the source's size and its mtime truncated to whole seconds. A re-injection loop writes several mutations of one file in quick succession; two mutations of the same kind — replacing a `continue` with a `break`, at two different lines — produce byte-identical file sizes. Both mutants landed at mtime second 1788400567 with size 31075, so the second import read the first's bytecode and the second mutant was never executed. Reported as SURVIVED. Swept the cache, and it was killed.

The blast radius is the same one 0.73.0 described for its own bug, which is the argument for fixing it the same week: a false-negative counterfactual reads as a green re-injection, becomes `fix_verified: true`, becomes a `confirmed` row in the permanent ledger, and is published in the Track record as a claim that held up. Silent, self-flattering, and in exactly the direction §9 exists to prevent. The remedy is one environment variable or one `rmtree`, and a check that can actually fail — which, honestly, means running the counterfactual and requiring the symptom to flip, because no static check tells fresh bytecode from stale.

Why this is not a re-report of VERDICT-F-43: F-43's hole is closed and I verified it independently (a `cp -a` copy of this checkout does import the original until PYTHONPATH is pinned, and does import itself afterwards). This is a second, unrelated mechanism that defeats the same procedure and that F-43's fix neither addresses nor detects. Different mechanism, different remedy, new id.

### VERDICT-F-42 — STILL_OPEN — Major/P1 — REAL_DEFECT

The strict bash guard still permits archiving the checkout away — 0.71.0 closed the exact command the finding quoted, but `tar -cf <scratch>/loot.tar --remove-files <checkout>/hooks` is allowed, because every option token containing the letter `f` is treated as taking an argument and `--remove-files` swallows the operand
- Seven-case probe against hooks/enforce_bash_scope.py at HEAD, VERDICT_STRICT=1, real git checkout as cwd: case B `tar -cf <scratch>/loot.tar --remove-files <repo>/hooks` returns rc=0 (ALLOW) where DENY is required; case C `tar -c -f <scratch>/loot.tar --remove-files <repo>/hooks` also rc=0.
- The reported shape is closed: case A `tar --remove-files -cf <scratch>/loot.tar <repo>/src` returns rc=2 (DENY) at HEAD and rc=0 (ALLOW) at a6011ec — the same probe run against `git show a6011ec:hooks/enforce_bash_scope.py`, where all four deletion cases were allowed (4 mismatches) against 2 at HEAD.
- Mechanism traced directly: `_check_tar(["-cf", "/s/loot.tar", "--remove-files", "/repo/hooks"], "/cwd")` yields `[]`; `_check_tar(["-c", "-f", "/s/loot.tar", "--remove-files", "/repo/hooks"], "/cwd")` yields `[]`.
- hooks/enforce_bash_scope.py:332-336 — `if t.startswith("-"): if "f" in t.lstrip("-"): next(it, None)`. `"--remove-files".lstrip("-")` is `"remove-files"`, which contains `f`, so the token after it is consumed as if it were an archive name.
- The same bug misreads the tested order too: `_check_tar(["--remove-files", "-cf", "/s/x.tar", "/repo/src"], "/cwd")` yields the archive `/s/x.tar` as a removal target as well as `/repo/src`. The passing test is right for half the wrong reason — `--remove-files` swallowed `-cf`, not the archive name.
- The gap is visible in this run's own coverage: facts.json per_file marks hooks/enforce_bash_scope.py lines 330-331 unexercised — the `if t in skip: next(it, None); continue` branch, which is exactly the split `-c -f` path case C walks. Production changed-line coverage fell 100% (run 9) to 94% (this run) on those two lines alone.
- Root cause: The new `--remove-files` operand loop parses tar options by substring rather than by an option table: any token starting with `-` whose remainder contains `f` is assumed to take a separate argument. `--remove-files` satisfies that test, so whatever follows it — including the checkout path it is about to delete — is skipped without being yielded as a candidate target. → ad05c6f (v0.71.0), the F-42 fix itself. The loop is new in that commit; `git diff a6011ec..HEAD -- hooks/enforce_bash_scope.py` shows it added whole.
- Class: {"pattern": "searched `hooks/enforce_bash_scope.py` for the same substring-option shape: two sites, both in `_check_tar`. Line 334 is this finding. Line 310-311 uses the identical idiom for `x` and pr

0.71.0 closed the command this finding quoted and left the hole it described. `tar --remove-files -cf <scratch>/loot.tar <checkout>/src` is now denied — I measured it allowed at a6011ec and denied at HEAD on the same probe, so the fix is real. Move `--remove-files` after the archive name, which GNU tar accepts and which nobody would think twice about writing, and the checkout can be archived out and deleted again.

The mechanism is one line. The new operand loop decides whether an option takes an argument by asking whether the letter `f` appears in it. `--remove-files` contains an `f`. So the token after it is skipped as if it were an archive name — and in the bypassing order, that token is the checkout path the command is about to delete. Nothing is yielded, nothing is checked, exit 0. The same bug is visible in the *passing* case: with `--remove-files` first, it swallows `-cf` and the guard ends up flagging the archive file itself as something being removed. The test passes on a coincidence.

Two things make this worth P1 rather than another Major. It is the profile's number-one risk area, where a bypass silently removes the product's core safety guarantee. And this run's own coverage measurement pointed straight at it before the probe did: hooks/enforce_bash_scope.py:330-331 are the only unexercised production lines in the range, they are the `-C`/`-f` skip branch, and they are exactly the path the split-flag bypass walks. The two-point production coverage story and the security finding are the same fact told twice.

I kept the id rather than minting a new one, following this project's own lesson of 2026-09-02: when a fix lands halfway, the wording changes and the file does not. Filing it fresh would have reset a known hole's age to zero and told a reader that a two-day-old defect was new.

### VERDICT-F-52 — NEW — Major/P2

The 0.73.0 prompt release says it was eval-paid, but no eval row was published for it, and the fixture it names cannot distinguish the new contract from the old — the seeded pricer key has no row that touches re-injection isolation and the fixture has no importable package to isolate
- CHANGELOG.md:37-40 — "**Prompt edits are eval-paid here, so this one was paid.** The seeded pricer eval scores **6/6 with no hard failures** against the edited prompt, matching the published baseline."
- eval/README.md's "Published results" table has no row for the 0.73.0 prompt. Its most recent row is dated 2026-09-01 for the v0.50.0 prompt; `grep -n "0\.7[0-3]" eval/README.md` returns no table row at all.
- `git diff a6011ec..HEAD --stat` shows d21d4f9 touching six files — .claude-plugin/plugin.json, CHANGELOG.md, agents/verdict.md, docs/state-schema.md, pyproject.toml, server.json. eval/README.md and eval/corpus/ are not among them, so neither a row nor an archived run was added.
- This project's own convention for the other two prompt releases was to publish: `git log --follow agents/verdict.md` shows exactly three touches in this era — 672dc98 (v0.47.0), 22c0e90 (v0.50.0), d21d4f9 (v0.73.0) — and both earlier ones carry dated eval/README rows, at n=3 with a byte-identical control prompt for v0.50.0.
- CHANGELOG.md:888-890 states the standard the 0.73.0 evidence falls short of: "a prompt edit is a behaviour change, so this release is gated on measurement, and the order was strict — measure the standing behaviour first, then edit, then measure again." No before-measurement is recorded for 0.73.0.
- The instrument cannot discriminate even in principle. eval/expected-delta.json's six rows are regressed-rounding, new-bulk-threshold, still-open-floor, resolved-env-fixture, quarantine-released-on-expiry and verdict — none scores whether a re-injection was isolated. `grep -il "pythonpath|isolat|re-inject" eval/expected*.json eval/score.py` returns nothing.
- And the failure mode cannot arise on that fixture: `ls eval/fixtures/pricer_rev_b` is CHANGELOG.md, README.md, pricer.py, test_pricer.py — no package, no virtualenv, no editable install, so there is no `.pth` for the new paragraph to protect against. A 6/6 there is a non-regression control, which is worth having and is not payment.
- Root cause: The release's central change is behavioural (a contract clause) and the only instrument that can see behaviour is a model run. One was reported in prose and not recorded anywhere a later reader can join it to a fixture, a model, a prompt version or an n; and the fixture named scores six rows, none of which can be affected by the clause under test. → d21d4f9 (v0.73.0). The claim and the omission arrive together.
- Class: {"pattern": "searched the changelog for the same claim shape: `grep -n \"eval-paid\" CHANGELOG.md` returns three sites \u2014 0.47.0 (line 886), the full_sweep paragraph (line 736) and this one (line 

Three prompt releases in this era, three claims of eval payment, and two published rows. 0.47.0 and 0.50.0 each added a dated row to eval/README.md naming the model, fixture, mode, prompt version and n — 0.50.0 at n=3 against a byte-identical control prompt, which is the standard 0.47.0's own text sets out: measure the standing behaviour, then edit, then measure again. 0.73.0 added no row, no corpus entry, and no control, and its changelog says the edit was paid for.

I want to be precise about what I am and am not claiming. I did not run the eval; the 6/6 may well have happened exactly as written. The finding is that nothing in the repository can be joined to it, in a project whose entire published discipline is that a claim and a measurement are different things — the same project that this week corrected a profile sentence and a changelog sentence for exactly that reason.

The harder half is not fixable by publishing. The seeded pricer key scores six rows: four delta classifications, a quarantine release and the verdict. None of them can be affected by a clause about isolating a scratch import path. And the fixture is four files — a CHANGELOG, a README, `pricer.py` and `test_pricer.py` — with no package and no virtualenv, so the `.pth` failure mode the clause exists to prevent cannot arise there at all. A 6/6 on that fixture is a useful non-regression control, and it is not evidence that the new contract works, because it would read 6/6 against the old contract too. Saying so costs nothing and stops the number being spent twice.

### VERDICT-F-26 — STILL_OPEN — Major/P1 — REAL_DEFECT

Fix verification picks its test by scraping any pytest node id out of a finding's prose evidence, so a mis-scraped id that fails at HEAD force-reopens a correctly-resolved finding over the tester's judgment
- Untouched by this range: `git diff a6011ec..HEAD -- src/verdict_mcp/harness.py` changes only `is_test_file`/`by_kind` (the F-44 split) and `_render_calibration` (the F-45 footnote). The selector at harness.py:329 and the `first_cited` path are byte-identical.
- It acted on live data again this run: facts.json `verification` contains one entry, for VERDICT-F-26 itself, selecting tests/test_calibration.py::test_the_tally_separates_what_was_measured_from_what_was_asserted with `selected_by: first_cited`, `candidates: 1`, at_previous `pass`, at_head `pass` — a measurement of a third party that settles nothing about F-26.
- facts.json verification_notes: "9 open finding(s) cite no test id, so the harness cannot fix-verify them" — the selector is all-or-nothing, so the same scraping is the only route to a machine verification.

Untouched by this range and now the oldest open Major. It acted again on live data: the only entry in this run's `verification` block is for F-26 itself, selecting a calibration test that has nothing to do with it, passing at both commits and settling nothing. Nine open findings cite no test id at all, so the scraper is the only route to a machine verification — which is why the explicit `verification_test` citation the last two runs have been writing by hand is not optional.

### VERDICT-F-43 — RESOLVED — Major/P2 — REAL_DEFECT

The re-injection procedure the agent contract mandates silently measures the unmodified original: a scratch copy made the way the guard was widened to allow carries `.venv`, whose editable-install `.pth` names the original checkout's `src` absolutely
- agents/verdict.md:221-227 now carries the step, the measurement (0 of 4 without, 4 of 4 with) and the check to run; docs/state-schema.md:319-326 carries the machine-side twin. Confirmed by `git diff a6011ec..HEAD -- agents/verdict.md docs/state-schema.md`.
- The mechanism it describes was reproduced independently this run rather than taken on the record's word: in a `cp -a` copy of this checkout, `<scratch>/.venv/bin/python -c "import verdict_mcp; print(verdict_mcp.__file__)"` printed `<ORIGINAL checkout>/src/verdict_mcp/__init__.py`; the same command with `PYTHONPATH=<scratch>/src` printed the scratch path. The advice is correct for this repository.
- fix_verified is false, deliberately: the fix is prose in a prompt, there is no guard test to re-inject against, and tests/test_agent_contract.py does not pin this paragraph — deleting it would break nothing. That is a known and previously reported condition of this file (its own docstring says so), not a new finding.
- The remedy is incomplete in a way its own check cannot detect — filed as VERDICT-F-50, which is a second, independent way the same procedure lies rather than a re-report of this one.

Resolved, and deliberately not fix-verified. The contract now names the isolation step, gives the measurement behind it and states the check; I confirmed the mechanism it describes is real in this checkout rather than taking the record's word — the scratch copy imports the original until PYTHONPATH is pinned, and imports itself afterwards. But the fix is prose in a prompt, there is no guard test to re-inject against, and nothing in tests/test_agent_contract.py pins the paragraph. Marking it verified would be the same overreach the finding was filed about. The incompleteness of the recipe is VERDICT-F-50, which is a different mechanism, not a re-report of this one.

### VERDICT-F-44 — RESOLVED — Major/P2

The diff-coverage percent blends test-file lines with production lines, and test-file lines carry a structural unexercised tax, so the run-over-run gate moves with the test/production composition of the diff rather than with coverage
- Re-injected in an isolated scratch copy: replacing `is_test_file`'s body with `return False` (src/verdict_mcp/harness.py:607-617) turns tests/test_diff_coverage.py::test_what_counts_as_a_test_file, ::test_the_production_percent_falls_when_production_coverage_falls and ::test_the_report_reads_production_first from `3 passed in 1.82s` into `2 failed, 1 passed in 1.40s`.
- The split is honest, checked rather than trusted: recomputing by_kind from facts.json per_file gives production 32/34 (94%) and tests 101/119 (85%), matching the reported numbers, and 34 + 119 = 153 = the blended denominator with no double counting.
- The classifier was audited over the whole repository, not the diff: of 74 tracked .py/.ts files exactly 35 classify as test (the 25 project tests, tests/conftest.py, 9 eval fixture test files and one .ts fixture test) and no production file is misclassified in either direction.
- It earned its keep on its first live run: production changed-line coverage reads 94% against run 9's 100%, and the two unexercised lines it names (hooks/enforce_bash_scope.py:330-331) are exactly the branch carrying the live guard bypass reported as VERDICT-F-42. The blended figure moved 78% to 87% over the same range and would have said the opposite.

Fix-verified, and the most useful thing in the range. The split is not a relabelling: I recomputed both halves from the per-file data and they reconcile exactly, and I ran the classifier over all 74 tracked source files to check that nothing production is being quietly counted as test — 35 test files, all of them actually tests, and no production file misclassified either way.

On its first live run the new production number went down, from 100% to 94%, and named hooks/enforce_bash_scope.py:330-331. Those two lines are the branch that lets the tar guard be walked past. The blended number over the same range went *up*, 78% to 87%. One of those two readings would have sent a reader looking and the other would have sent them home.

### VERDICT-F-22 — STILL_OPEN — Minor/P2 — age 1d

The VERDICT_STRICT bash guard reads shell metacharacters inside quoted strings and heredoc bodies as live syntax and resolves phantom targets against the checkout, blocking read-only QA commands
- Four blocked read-only commands this run, each costing a round trip. (1) A Python format spec `{rc:>3}` inside a single-quoted heredoc body: "output redirection targets '<checkout>/3} (a git checkout under /private/tmp)'".
- (2) A string literal `"<tmp>"` inside the same kind of heredoc: "output redirection targets '<checkout>/\"))'".
- (3) A `->` inside a module docstring: "output redirection targets '<checkout>/the'".
- (4) NEW SHAPE, same family: `cd <scratchpad> && cat > trace2.py <<'EOF'` — a genuine redirect whose relative target was resolved against the tool's cwd rather than the `cd` in the same command, so a write to a temp scratch dir was refused as a write to the checkout: "output redirection targets '<checkout>/trace2.py'". Folded in here rather than given a new id because the harm and the file are the same; noted as a distinct mechanism.
- Workaround used all four times: literal absolute paths and no `<`, `>` or `->` anywhere in a heredoc body. The guard's refusal of an unexpanded `$S/...` variable is by contrast a deliberate, well-messaged fail-closed and is not part of this finding.

Four blocked read-only commands this run, all four in service of probing the guard that blocked them. Three are the known shape — a `>` inside a quoted string or heredoc body read as live redirection, resolved into a phantom path under the checkout — arriving from a Python format spec (`{rc:>3}`), a string literal (`"<tmp>"`) and a docstring arrow (`->`). The fourth is a mechanism I have not seen recorded before and folded in rather than filed fresh: a genuine redirect with a relative target, resolved against the tool's working directory instead of the `cd` immediately preceding it in the same command, so a write to a temp scratch directory was refused as a write to the checkout.

Still Minor: it over-blocks, never under-blocks, and every instance had a workaround. But it has now taxed three consecutive runs, and a guard that a QA session must write around is one a QA session will eventually write around carelessly. If it costs a fifth command next run, raise it.

### VERDICT-F-49 — NEW — Minor/P2

The same substring option parsing in the other direction: any tar flag containing the letter `x` reads as an extraction, so `tar --exclude=.venv -cf <scratch>/x.tar <checkout>/src` is denied as an overwrite of the current directory
- `_check_tar(["--exclude=.venv", "-cf", "/s/x.tar", "/repo/src"], "/cwd")` yields `[('tar extract (overwrites in place)', '/cwd')]` — a create is reported as an extraction, and the flagged target is the cwd, which for a QA session is the checkout.
- `_check_tar(["-cf", "/s/x.tar", "--exclude", "junk", "/repo/src"], "/cwd")` yields the same.
- hooks/enforce_bash_scope.py:310-311 — `extracting = (any(t.startswith("-") and "x" in t.lstrip("-") for t in args) ...)`. `"--exclude=.venv".lstrip("-")` contains `x`.
- Archaeology: `git log -L 310,311:hooks/enforce_bash_scope.py` names 27eabd5 (v0.45.0) as the commit that introduced this line. It predates the 0.71.0 tar work and was not touched by it.
- Practical cost: `tar --exclude=.venv -cf <scratch>/backup.tar <checkout>` is the natural way to snapshot a checkout for a re-injection without dragging 49 MB of virtualenv along — the exact operation the contract's scratch-copy step asks for, refused.
- Root cause: Extraction is detected by testing whether the letter `x` appears anywhere in an option token's body, which is sound for bundled short flags (`-xzf`) and wrong for every long option containing an x. `--exclude`, `--xattrs` and `--extract`-adjacent spellings all match; the handler then reports an extract whose target defaults to the cwd. → 27eabd5 (v0.45.0), per `git log -L 310,311:hooks/enforce_bash_scope.py`.
- Class: {"pattern": "the mirror of VERDICT-F-42's line 334. `grep -n 'lstrip(\"-\")' hooks/enforce_bash_scope.py` returns exactly these two sites, and both should be replaced by the same option table.", "site

The mirror image of VERDICT-F-42, eight lines earlier and eighteen releases older. Extraction is detected by looking for the letter `x` anywhere in an option token, which is right for `-xzf` and wrong for `--exclude`. So `tar --exclude=.venv -cf <scratch>/backup.tar <checkout>` — the natural way to snapshot a checkout for a re-injection without dragging fifty megabytes of virtualenv along, which is to say the exact operation §3 asks a tester to perform — is read as an extraction and denied against the current directory.

Harmless in the safety direction and pure friction in the other, which is why it is Minor. It belongs in the same change as F-42 because it is the same mistake, and because fixing one of the two lines and leaving the other would be the third patch to this handler in three releases.

### VERDICT-F-51 — NEW — Minor/P2

The footnote 0.72.0 added to close VERDICT-F-45 reports the largest per-bucket count where its sentence scopes over every confirmation, so the published table says `Up to 17` of 28 confirmations are unrecorded when 21 of them are
- Rendered from the committed .qa/state.json through `verdict_mcp.harness._render_calibration`, the published line reads: "*Up to 17 of those confirmations were settled before `outcome_basis` was recorded and are neither kind. They are counted in `held up` and in no other column…*"
- The same state's per-bucket arithmetic: proven 24 confirmed − 0 measured − 7 claimed = 17; unstated 3 − 0 − 0 = 3; probable 1 − 0 − 0 = 1. Sum 21, maximum 17, total confirmed 28.
- src/verdict_mcp/harness.py:1838-1839 — `unrecorded = max((v.get("confirmed", 0) - v.get("confirmed_measured", 0) - v.get("confirmed_claimed", 0) for v in buckets), default=0)`. The generator is per bucket; the sentence it feeds says "those confirmations", which is the whole `held up` column.
- Reader consequence, which is precisely what VERDICT-F-45 was filed about: the table shows 28 held up and 0 measured, so subtracting the stated 17 leaves 11 that appear to be measured or the tester's word, when the true attributable count is 7 — all `claimed`, all in the proven row.
- The fix is otherwise sound and fix-verified this run: re-injecting `if unrecorded:` as `if False:` fails tests/test_calibration.py::test_the_report_says_how_many_confirmations_predate_the_basis while the negative control test_no_such_line_when_every_confirmation_carries_a_basis still passes.
- Root cause: `max` over the buckets answers "what is the largest number of unrecorded confirmations in any one row"; the sentence asks "how many of the confirmations in this table are unrecorded". Those coincide only when a single bucket carries them all, which is true for by_proof_method (empty) and false for by_confidence here. → b814a24 (v0.72.0), the VERDICT-F-45 fix.
- Class: {"pattern": "searched `src/verdict_mcp/harness.py` for other cross-bucket aggregates in the renderer: `grep -n 'max(\\|sum(' src/verdict_mcp/harness.py` in `_render_calibration` returns only this one;

The third pass at one table. 0.64.0 split measured from claimed in the state; 0.70.0 printed the split; 0.72.0 added the footnote for rows that predate the field. The footnote now takes the largest per-bucket count where its sentence covers the whole column, so it says 17 where the answer is 21. A reader doing the arithmetic the footnote invites — 28 held up, 0 measured, up to 17 unrecorded — concludes 11 confirmations are attributable when 7 are. Small, and precisely the failure mode of the finding it fixes.

The fix itself is sound and I verified it by re-injection; what its test does not do is check the number against anything. Any aggregation returning a positive integer passes it.

### VERDICT-F-54 — NEW — Minor/P2

Every finding's `first_seen` and `age_days` are stamped from the machine's local calendar while the run that stamps them is timestamped in UTC, so this run filed five findings dated 2026-09-02 inside a state whose own timestamp, report filename and INDEX row all read 2026-09-03
- Found by the mandated pre-handoff self-check (contract §13) reading back the state this run had just written, and reproduced before filing.
- src/verdict_mcp/harness.py:1214 `today = today or date.today()`. `date.today()` is the local calendar; every other clock in this module is UTC — harness.py:845 `datetime.now(timezone.utc)`, harness.py:1657 and 1676 likewise, and `last_run.timestamp_utc` by name.
- Measured on this machine at the moment of the run: `date.today()` returns 2026-09-02 while `datetime.now(timezone.utc).date()` returns 2026-09-03.
- The artifacts disagree with each other as a result. In the first run-10 state written this session, `last_run.timestamp_utc` was 2026-09-03T01:44:59Z, the report was reports/2026-09-03-delta-run10.md and the INDEX row read 2026-09-03, while VERDICT-F-50 — filed for the first time by that same run — carried `first_seen: 2026-09-02`.
- Ages inherit the error: VERDICT-F-21 has `first_seen: 2026-09-01` and was stamped `age_days: 1` when its true age at the run's own UTC timestamp is 2 days. §6 of the contract calls age the pressure and requires it to be shown, and the report prints this number.
- harness.py:1246 makes the stamp permanent: `first_seen = (prior or {}).get("first_seen") or today.isoformat()`, and the value is copied into the permanent ledger, so a wrong date is carried for the life of the finding rather than recomputed.
- Root cause: `merge()` defaults its `today` argument to `date.today()`, which resolves against the host's timezone. For any run executed between 00:00 and 07:00 UTC on a US Pacific machine — that is, any evening run — the local calendar is one day behind the UTC calendar the rest of the run is measured in, so newly filed findings are dated to the previous day and every age is computed one day short. → Predates this range: `git diff a6011ec..HEAD -- src/verdict_mcp/harness.py` does not touch line 1214. It has been latent because every prior run of this project was made in local daytime, where the two calendars agree; run 10 at 01:44Z is the first to cross UTC midnight.
- Class: {"pattern": "searched the module for other local-clock reads: `grep -n 'date.today()\\|datetime.now(' src/verdict_mcp/harness.py` returns four sites \u2014 845, 1657 and 1676 all pass `timezone.utc`, 

The pre-handoff self-check earned its place again. This project's lessons ledger already records, from run 4, that the check exists because two harness defects were sitting in the artifacts that run had just produced and neither would have been found by reading source. Same again: I read the written state back, noticed that findings filed by a run timestamped 2026-09-03T01:44:59Z were dated 2026-09-02, and found a naive `date.today()` behind it.

It is one line and one timezone, and it is the first run in this project's history made after UTC midnight, which is why nine previous runs agreed with themselves. The consequence is small per run and permanent per finding: `first_seen` is stamped once and carried into the outcome ledger, so a finding filed today will claim yesterday for as long as the project keeps records, and every age computed from it is a day short.

The sharpest bit is that half of this pair was already fixed. The INDEX row's date was moved to the state's UTC timestamp after VERDICT-F-24, and there is a test guarding it. The findings' dates were not, so the two halves of the same artifact now disagree — which is exactly the condition that test was written to catch, one field over.

Filing this cost a second finalize: the state, ledger, history row and INDEX row written before the finding was found were restored to their pre-run values from git and the run was finalized once, so the record carries one run 10, not a run 10 and a correction.

### VERDICT-F-53 — NEW — Minor/P3

The abandoned-run marker narrates any marker up to six hours old as "minutes ago", contradicting the age it records in the same object — this run's facts read `age_hours: 2.01` beside "minutes ago … Nothing is missing"
- This run's .qa/facts.json: `"previous_attempt_this_run": {"started_utc": "2026-09-02T23:44:23Z", …, "age_hours": 2.01, "meaning": "an earlier attempt at this same commit, minutes ago — this run's own retry, not a lost run. Nothing is missing"}`.
- src/verdict_mcp/harness.py:81 `RETRY_WINDOW_HOURS = 6` and harness.py:994 `age_h is not None and age_h <= RETRY_WINDOW_HOURS`, with the fixed string "minutes ago" at harness.py:999. Any marker from 0 to 6 hours old is described as minutes old.
- The marker this run met was in fact two hours old and from a different session (the file is gitignored per-run scratch, `.qa/.gitignore:5`), so it was a crashed or superseded attempt rather than a retry of the run that read it. The chain conclusion happens to hold — nothing was finalized, so nothing is missing from runs.jsonl — but the harness reaches it from a description that is false.
- Same shape as the footnote defects this project has already fixed twice (VERDICT-F-36, VERDICT-F-45): a rendered sentence asserting something the measured value beside it contradicts.
- Root cause: The narration string is written for the middle of the retry window rather than derived from the measured age, and the window is six hours wide. → The retry-window branch in `facts()`; unchanged by this range, so it predates a6011ec.
- Class: {"pattern": "searched the harness for other hardcoded time words beside computed ages: `grep -n 'minutes ago\\|hours ago\\|just now' src/verdict_mcp/harness.py` returns only harness.py:999. The siblin

A sentence that contradicts the number beside it, in the artifact whose job is telling the next run what happened to the last one. The retry window is six hours; the narration says "minutes ago" for all of it. This run's marker was two hours old and from a different session, and the conclusion the harness drew from it — nothing is missing — happens to be correct, because the abandoned attempt finalized nothing. It got there through a false description, and the next reader has no way to tell a correct conclusion from a lucky one.

### VERDICT-F-45 — RESOLVED — Minor/P2

The Track record's new `of those, measured` column counts a row written before `outcome_basis` existed as not-measured, under a footnote asserting the two kinds are exhaustive
- Re-injected in an isolated scratch copy: `if unrecorded:` at src/verdict_mcp/harness.py:1848 replaced by `if False:` turns tests/test_calibration.py::test_the_report_says_how_many_confirmations_predate_the_basis and ::test_no_such_line_when_every_confirmation_carries_a_basis from `2 passed in 0.04s` into `1 failed, 1 passed in 0.05s` — the negative control still passing, so the pair is not asserting vacuously.
- The rendered footnote is present in the current state's Track record output and says what the finding asked for: that the rows predating `outcome_basis` are counted in `held up` and in no other column.
- The number it prints is wrong for a reason the fix's test cannot see, filed as VERDICT-F-51.

### VERDICT-F-46 — RESOLVED — Minor/P3

The permanent-ledger verification digest drops `not_weighed` and `candidates`, so a confirmed row that stands over a measurement the harness declined to weigh is recorded forever as a confirmation beside a test that failed at HEAD with nothing saying why it did not count
- Re-injected in an isolated scratch copy: restoring the old four-key tuple in `_verification_digest` (src/verdict_mcp/state.py:231-232) turns tests/test_calibration.py::test_the_digest_keeps_what_makes_the_selection_readable from `1 passed in 0.04s` into `1 failed in 0.05s`.
- `git diff a6011ec..HEAD -- src/verdict_mcp/state.py` shows `candidates` and `not_weighed` added to the retained keys, with the reason recorded in the comment above them.

### VERDICT-F-47 — RESOLVED — Minor/P3

The fixture-freshness gate's symlink blind spot is fixed but untested, while CHANGELOG.md claims "four now, one per blind spot"
- Re-injected in an isolated scratch copy: `shutil.copy2(REPO / rel, dest, follow_symlinks=False)` at eval/fixture_freshness.py:76 replaced by `shutil.copyfile(REPO / rel, dest)` turns tests/test_fixture_freshness.py::test_a_symlink_swapped_for_a_regular_file_is_no_longer_invisible and ::test_a_mode_change_is_no_longer_invisible from `2 passed in 0.59s` into `2 failed in 0.61s`.
- The overstated claim was corrected in place rather than left: CHANGELOG.md's 0.70.0 entry now reads "a green-path control plus one each for the mode change, the planted untracked file and the missing tracked file" and names the symlink case as covered by 0.72.0.
- Verified on POSIX only — the new test is skipped on Windows by design (`os.name == "nt"`), and Windows is in the CI matrix but not testable here.

### VERDICT-F-48 — RESOLVED — Minor/P2

Five non-equivalent operator and guard mutants in validate.py survive the whole 674-test suite — including the judgment validator's status-enum check and both of the PostToolUse hook's exit codes
- All five re-applied by hand in an isolated scratch copy, each against the guard test written for it, with the instrument controlled first: unmutated, all five guards pass (`1 passed` each); mutated, all five fail (`1 failed` each). Killed 5 of 5.
- The five: validate.py:214 `not in` becomes `in` (guard test_judgment_status_must_be_one_the_contract_knows); validate.py:198 `continue` becomes `break` (test_judgment_keeps_checking_after_a_malformed_entry); validate.py:429 `continue` becomes `break` (test_the_state_validator_keeps_checking_after_a_malformed_entry); validate.py:551 `return 2` becomes `return 0` (test_hook_mode_refuses_a_state_it_cannot_read); validate.py:556 `return 0` becomes `return 1` (test_hook_mode_is_silent_over_a_clean_state).
- The scratch tree was restored byte-identically after every injection (`tree restored byte-identical: True`), and the module under test was confirmed to be the scratch's own (`verdict_mcp.validate.__file__` printed inside the scratch).
- The first pass at this measurement read 4 of 5 — M3 SURVIVED — and that reading was wrong: it was running an earlier mutant's cached bytecode. Filed as VERDICT-F-50, and recorded in lessons.md, because the same error would have published a false survivor against a guard that works.
- The profile's overstated summary ("mostly message text") was corrected in .qa/profile.md by 0.72.0, which is the other half of what this finding asked for.

Fix-verified 5 of 5, and the reason that took two passes is filed as VERDICT-F-50. The first measurement read 4 of 5 with M3 surviving; the survivor was an artefact of a stale `.pyc`, not a gap in the suite. Both passes are reported because the difference between them is the whole point: identical scratch tree, identical PYTHONPATH, identical `__file__`, opposite answer.

The five guards themselves are good ones. Each passes unmutated and fails mutated, so none is passing vacuously, and they cover the field that decides whether a finding is open, the two loops that would stop checking after the first malformed entry, and both exit codes of the hook that catches a bad state while the run is still going.


## Track record

53 findings tracked across this project's history · 33 settled, 20 still undecided.

| Confidence claimed | Held up | of those, measured | Withdrawn | Rate |
|---|---|---|---|---|
| proven | 29 | 0 | 0 | _not yet_ |
| unstated | 3 | 0 | 0 | _not yet_ |
| probable | 1 | 0 | 0 | _not yet_ |

*A rate appears once a row has 30 settled outcomes. Settled means fix-verified or regressed (it held up) against withdrawn (it did not); a resolution nobody checked at all settles nothing.*

*`held up` covers two things and they are not equal: a re-injection the harness measured, and one the tester asserted where no measurement contradicts it. The `measured` column is the first kind. Read them apart.*

*Up to 17 of those confirmations were settled before `outcome_basis` was recorded and are neither kind. They are counted in `held up` and in no other column, because calling them the tester's word would be a claim of its own.*

## Release blockers

_None._

## Verified intact

- The suite is green and grew honestly: 690 passed / 0 failed / 0 skipped in 74.55s, 16 test ids added and 0 removed against run 9's 674 (facts.json test_ids set-diff, not summary arithmetic).
- Suite duration 74.82s against run 9's 72.17s, +3.7% — inside the 10% week-over-week gate.
- Fixture freshness gate passes: `python3 eval/fixture_freshness.py` exit 0, "pricer-delta.diff still describes the fixture pair (78 lines)". The tamper anchor over the seeded-defect fixtures is intact.
- The diff-coverage production/test split is arithmetically honest, not a relabelling: recomputing by_kind from facts.json per_file gives production 32/34 and tests 101/119, matching the reported 94% / 85% / 87% blended exactly.
- The classifier behind that split was audited across the whole repository, not just the diff: of 74 tracked .py/.ts files, exactly the 25 project tests, tests/conftest.py and 10 eval fixture test files classify as test; no production file is misclassified in either direction.
- Five of five mutants that 0.72.0 claims are now watched die against their named guard tests, with the bytecode cache controlled (M1 status enum, M2/M3 loop continues, M4/M5 both PostToolUse exit codes) — and all five pass unmutated first, so the guards are not passing vacuously.
- Four of run 9's remaining code fixes are fix-verified by isolated re-injection: F-44, F-45, F-46, F-47 each pass at HEAD and fail when the defect is put back.
- The `tar` guard's original reported shape is genuinely closed: `tar --remove-files -cf <scratch>/loot.tar <checkout>/src` is ALLOWED at a6011ec and DENIED at HEAD, measured on the same probe against both commits.
- The guard's pre-existing behaviour did not regress: `tar -cf <scratch>/backup.tar <checkout>/src` still allowed, `tar -xf <scratch>/x.tar -C <checkout>/src` still denied, `tar --remove-files -cf <scratch>/x.tar <scratch>/stuff` still allowed.
- The harness's own machine-side verifier does isolate: facts.json `verification.VERDICT-F-26.pythonpath` records two scratch paths, confirming that F-43 was only ever an agent-side gap.
- The code census over the range is clean — no undeclared imports, and the four `for_now` placeholder hits are all inside `.qa/` prose from run 9, not in shipped code.
- The run-history chain is unbroken: run 9's recorded chain `60a6773f…` is carried and this run appends to it rather than re-baselining.

## Not tested

- Windows and the py3.10 leg of the CI matrix — the profile names cross-platform as risk area 5 and this machine is macOS/py3.13 only. `test_a_symlink_swapped_for_a_regular_file_is_no_longer_invisible` is skipped on Windows by design, so the F-47 fix is verified on POSIX only.
- The eval corpus was not run. No model run was made this run (it costs API tokens and the range's prompt change is judged from the record, not re-measured); the 6/6 claim in the 0.73.0 changelog is therefore assessed for auditability and discriminating power, not reproduced. See VERDICT-F-52.
- A full mutmut campaign over src/verdict_mcp/validate.py was not re-run. Five named mutants were re-applied by hand with the instrument controlled; the other ~569 were not, so the killed/survived totals in the profile are unrefreshed this run.
- src/verdict_mcp/harness.py has still never been mutation-measured, and it is this project's top defect cluster.
- The MCP server surface (server.py) and gate.py were untouched by the range and were not exercised beyond the suite.
- The remaining ~569 non-re-applied mutants aside, no structural coverage technique (MC-DC, data-flow) was applied to the new `_check_tar` argument loop; the two-line gap was found from the diff-coverage report and then probed by hand.
- VERDICT-F-21's remedy is a maintainer decision, not a measurement; nothing new was measured against it this run.
- The 22 files in the range include 8 .qa/ artifacts from run 9 itself (commit 3bbce81). Those were read as the baseline record, not audited as code.
- No performance, concurrency or resource-exhaustion testing: out of scope for a plugin with no runtime service.

## Fix order

1. **VERDICT-F-42 and VERDICT-F-49 together, as one change.** They are the same nineteen lines and the same mistake — options parsed by substring. Replace both tests with a small option table (`-f/--file` and `-C/--directory` take an argument; nothing else in tar's create path does), and add at least one ordering the author did not pick, because the whole reason F-42 reopened is that the fix was tested against the sentence rather than the behaviour. Do these first: they are the only findings that touch the safety guarantee, and F-49 cannot be fixed after F-42 without touching the same loop twice.
2. **VERDICT-F-50.** Amend §3's isolation paragraph and its docs/state-schema.md twin to sweep `__pycache__` (or set `PYTHONDONTWRITEBYTECODE`), and replace the `__file__` check with one that can actually fail — the honest version is to run the counterfactual and require the symptom to flip, since no static check distinguishes fresh bytecode from stale. Take the machine-side belt at the same time: `_run_test`'s env in harness.py already sets PYTHONPATH and can set PYTHONDONTWRITEBYTECODE for free. Second because it is what makes every later verification, including the ones that will close item 1, trustworthy.
3. **VERDICT-F-52.** Publish the eval row for the 0.73.0 prompt, or correct the sentence that says the edit was paid for. This is bounded work and it decides how much of this project's own process record is worth. The harder half — that no row in the seeded pricer key can be affected by an isolation clause — should be recorded as known rather than quietly carried.
4. **VERDICT-F-51.** Sum the per-bucket unrecorded counts instead of taking their maximum, and make the test assert the number against an independently computed total rather than the presence of a line.
5. **VERDICT-F-54.** Pass the run's own UTC date into `merge()` — facts.json already carries `measured_at` — and assert that a NEW finding's `first_seen` matches the run timestamp's date. Cheap, and it stops a permanent field being stamped wrong.
6. **VERDICT-F-53.** Derive the phrase from the measured age or drop the time word.
7. **VERDICT-F-21** needs the maintainer's decision, not a fix, and it has needed it for two runs. **VERDICT-F-26** is now the oldest open Major and is a prerequisite for trusting any machine fix-verification, so it should be scheduled before the next release that expects one. **VERDICT-F-22** is friction rather than danger and can wait, but it has now cost four commands in one run.

## Next run focus

- VERDICT-F-50 first: check whether §3's isolation paragraph grew a bytecode clause and whether its stated check became one that can actually fail. The regression probe is the one this run ran — two mutations of the same file with identical byte size written inside one second, expecting the second to bite. A `print(pkg.__file__)` that still passes in both directions means the check was not strengthened, only the prose.
- VERDICT-F-42 second: re-run the seven-case tar probe. Cases B (`tar -cf <scratch>/x.tar --remove-files <checkout>/hooks`) and C (split `-c -f`, same suffix) must DENY, while E and G must still ALLOW. If the fix keys on `--remove-files` position rather than on proper option-argument parsing, it will pass B and fail some fourth ordering — so add at least one ordering the fix's author did not choose.
- VERDICT-F-49 belongs in the same change as F-42 and should be checked with it: `tar --exclude=.venv -cf <scratch>/x.tar <checkout>/src` must be ALLOWED. Today the `x` in `exclude` reads as an extract flag and the command is denied against the cwd.
- VERDICT-F-52 needs an artifact, not an argument: either an eval/README.md row dated for the 0.73.0 prompt naming model, fixture, mode, n and score, or a correction of the changelog sentence that claims the edit was eval-paid. Then answer the harder half — the seeded pricer key has no row that touches re-injection isolation and the fixture has no editable install, so no score on it can distinguish the 0.73.0 contract from the 0.72.0 one. A fixture that can is new work, and worth naming as such.
- VERDICT-F-54: confirm `merge()` takes the run's UTC date rather than `date.today()`, and add the check that would have caught it — a NEW finding's `first_seen` must equal the date of `last_run.timestamp_utc`. The reproduction needs no fixture: run finalize on a host whose local date is behind UTC, or pass a `today` a day off and assert the state refuses itself.
- VERDICT-F-51: the Track record footnote should sum the per-bucket unrecorded counts, not take their maximum. Today's state renders `Up to 17` where 21 of 28 confirmations are unrecorded; the regression test is a calibration block with two buckets both carrying unrecorded rows.
- VERDICT-F-26 is now the oldest open Major and was untouched by this range. It acted on live data again this run: the harness picked `test_the_tally_separates_what_was_measured_from_what_was_asserted` for F-26 itself, pass/pass, settling nothing.
- VERDICT-F-22 cost four blocked commands this run (a `>` inside a `{rc:>3}` format spec, a `<tmp>` inside a quoted string, a `->` inside a docstring, and a relative redirect resolved against the tool cwd rather than the `cd` in the same command). If it costs a fifth next run, raise it to Major — three consecutive runs of measurable friction is no longer a Minor.
- Re-run the mutmut campaign over src/verdict_mcp/validate.py and compare against run 9's 574 mutants / 391 killed. Control the instrument first as the profile requires — and this run's evidence says the control must now include sweeping `__pycache__`, not only checking the import path.
- Consider the first mutation campaign over src/verdict_mcp/harness.py. It is this project's top defect cluster by finding count and has never been mutation-measured; both of this run's renderer findings (F-51, F-53) live there.

## Notes

**On grading the graders.** Three of this run's conclusions came from refusing to let a fix's own machinery certify it. The tar fix's three new tests all pass and the hole is open, because they share the finding's token order. The mutation campaign's five guards all pass and the first re-measurement still read one as a survivor, because the instrument was uncontrolled. The 0.73.0 eval score is a real number that cannot answer the question it is offered for, because the fixture has nothing to isolate. In each case the artifact that would normally end the enquiry — a green test, a 5-of-5, a 6/6 — was the thing under test.

**On the one that nearly went the other way.** The first mutant pass reported M3 as a survivor and I was one step from filing "VERDICT-F-48 is only four-fifths closed" against a guard test that works perfectly. What stopped it was cheap: the *pattern* was wrong. Four of five biting and one not, in a module where the other two mutants of the same shape both bit, is more likely an instrument fault than a coverage gap — the same reasoning run 9 recorded when four of four passing turned out to be an instrument reading zero. Re-running with the cache swept settled it in one command. The lesson is filed.

**On what the new coverage split bought.** Run 8 declared the coverage gate live, run 9 found it measuring diff composition, and 0.71.0 split it. On its first run after the split, the production number fell 100% to 94% and named two lines — and those two lines are the branch through which the tar guard can be walked past. A blended reading would have shown 78% rising to 87% and congratulated everyone. That is what a gate is for, and it is worth saying plainly on a run where most of the news is about claims that outran their evidence.

**What I did not do.** I did not run the eval, so VERDICT-F-52 is a finding about a record, not a contradiction of a score: it is entirely possible the 6/6 was measured exactly as described. I did not re-run the full mutmut campaign, so the profile's 574/391 stands unrefreshed. And VERDICT-F-43 is resolved with `fix_verified: false` on purpose — its fix is prose with no guard to re-inject against, and calling that verified would be the same overreach the finding was about.

**On finding it in my own output.** VERDICT-F-54 was not found by reading the range; it was found by reading back the state this run had just written and noticing that findings filed on 2026-09-03 were dated 2026-09-02. That is the §13 check doing precisely what this project's lessons ledger says it is for, and it is the second time it has caught a clock defect in the harness. I restored the artifacts to their pre-run values from git and finalized once rather than filing a correction generation, so the history carries a single run 10.

---

*Countable sections rendered from `state.json` by `verdict-finalize`; the prose is the agent's. They cannot disagree.*
