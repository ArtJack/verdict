diff --git a/pricer/CHANGELOG.md b/pricer_rev_b/CHANGELOG.md
index af19420..6909d33 100644
--- a/pricer/CHANGELOG.md
+++ b/pricer_rev_b/CHANGELOG.md
@@ -1,5 +1,10 @@
 # Changelog
 
+## 2026-08-30
+
+- Test maintenance: net-proceeds expectation updated to the 12% fee (PRC-142),
+  bulk-orders fixture data added, bulk-discount test made deterministic.
+
 ## 2026-08-01
 
 - Marketplace fee raised from 10% to 12% (`FEE_RATE` 0.10 → 0.12). Intended product
diff --git a/pricer_rev_b/fixtures/bulk_orders.json b/pricer_rev_b/fixtures/bulk_orders.json
new file mode 100644
index 0000000..3195279
--- /dev/null
+++ b/pricer_rev_b/fixtures/bulk_orders.json
@@ -0,0 +1,6 @@
+[
+  {"price": 20.00, "qty": 15, "expected": 18.00},
+  {"price": 20.00, "qty": 10, "expected": 18.00},
+  {"price": 19.99, "qty": 12, "expected": 17.99},
+  {"price": 5.00, "qty": 9, "expected": 5.00}
+]
diff --git a/pricer/pricer.py b/pricer_rev_b/pricer.py
index e01a6e9..88e0c04 100644
--- a/pricer/pricer.py
+++ b/pricer_rev_b/pricer.py
@@ -27,6 +27,6 @@ def net_proceeds(price):
 
 def bulk_unit_price(price, qty):
     """Discounted unit price for bulk orders (spec rule 4)."""
-    if qty >= 10:
+    if qty > 10:
         return round_cents(price * 0.9)
     return price
diff --git a/pricer/test_pricer.py b/pricer_rev_b/test_pricer.py
index 89a6e97..a4e6cbb 100644
--- a/pricer/test_pricer.py
+++ b/pricer_rev_b/test_pricer.py
@@ -1,6 +1,5 @@
 import json
 import os
-import time
 
 import pytest
 
@@ -22,8 +21,8 @@ def test_listable_at_floor_exactly():
 
 
 def test_net_proceeds_hundred():
-    # fee is 10%
-    assert net_proceeds(100.0) == 90.0
+    # fee is 12% since PRC-142 (see CHANGELOG)
+    assert net_proceeds(100.0) == 88.0
 
 
 def test_round_cents_half_up():
@@ -32,8 +31,8 @@ def test_round_cents_half_up():
 
 
 def test_bulk_discount_applies():
-    qty = 9 + (time.time_ns() // 1000) % 2
-    assert bulk_unit_price(20.00, qty) == 18.00
+    # deterministic since the 2026-08-30 test maintenance pass
+    assert bulk_unit_price(20.00, 10) == 18.00
 
 
 def test_negative_price_message():
@@ -47,4 +46,4 @@ def test_bulk_orders_fixture():
     with open(path) as f:
         orders = json.load(f)
     for order in orders:
-        assert bulk_unit_price(order["price"], order["qty"]) <= order["price"]
+        assert bulk_unit_price(order["price"], order["qty"]) == order["expected"]
